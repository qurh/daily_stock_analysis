# 文档编写规范

## 必备要素

每份执行性文档至少包含：
- 背景
- 范围（包含非目标）
- 任务清单
- 验收标准
- 风险与回滚

## 写作约束

- 用短句，避免模糊词（如“尽快”“后续考虑”）。
- 所有任务必须有明确产物（文件、接口、测试、命令）。
- 验收必须可验证（命令输出、接口响应、截图、日志）。

## 示例

- 差：优化 API。
- 好：为 `/reports` 增加 `code` 与 `report_date` 过滤并补测试用例。
