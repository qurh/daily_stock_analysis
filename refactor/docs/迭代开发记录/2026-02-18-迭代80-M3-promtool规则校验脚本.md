# 迭代开发记录

迭代编号：`迭代80`  
日期：`2026-02-18`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 在后端 CI 流程中自动执行 Prometheus 规则校验。
2. 提供独立脚本，便于本地和流水线复用。
3. 为脚本接入增加自动化测试约束。

## 2. 计划范围（Plan）

1. 先新增失败测试，约束 `ci.sh` 调用规则校验脚本。
2. 新增 `check-prometheus-rules.sh` 并接入 `ci.sh`。
3. 同步版本、README、CHANGELOG 与迭代记录。

## 3. 实际完成（Done）

1. 新增脚本：
   - `refactor/backend/scripts/check-prometheus-rules.sh`
2. CI 接入：
   - `refactor/backend/scripts/ci.sh` 增加 `./scripts/check-prometheus-rules.sh`
3. 脚本行为：
   - 默认使用 `promtool`，可通过 `PROMTOOL_BIN` 覆盖。
   - 当本机未安装 `promtool` 时，输出 warning 并跳过校验，不阻塞本地开发。
4. 测试增强：
   - `test_ci_script_invokes_prometheus_rules_check`
5. 版本升级：
   - `0.3.65-m3-promtool-rules-check-script`

## 4. 未完成项（Not Done）

1. 暂未在 CI 环境强制要求 `promtool` 可用。
2. 暂未新增 `promtool` 版本约束检查。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/scripts/check-prometheus-rules.sh`
   - `refactor/backend/scripts/ci.sh`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-18-迭代80-M3-promtool规则校验脚本.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（先失败后通过）
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（规则校验脚本已接入质量流程）

## 7. 风险与问题

1. 风险描述：本地无 `promtool` 时仅跳过校验，无法发现规则语法问题。
2. 影响范围：本地自检完整性。
3. 缓解措施：后续在正式 CI 环境安装并强制执行 `promtool`。

## 8. 关键决策

1. 决策内容：采用“本地可跳过 + CI 可强制”的渐进式策略。
2. 决策原因：保证开发体验，同时不阻断后续严格化。
3. 影响模块：脚本工具链与持续集成流程。

## 9. 下迭代计划

1. 增加 `promtool` 强制模式开关（如 `PROMTOOL_REQUIRED=1`）。
2. 将规则校验结果纳入统一质量报告输出。
3. 补充 `promtool` 安装指引（README）。
