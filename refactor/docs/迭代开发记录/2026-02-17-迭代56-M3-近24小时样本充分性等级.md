# 迭代开发记录

迭代编号：`迭代56`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 补齐近24小时样本充分性的离散等级观测能力。
2. 让近期样本质量告警可同时使用 one-hot 标签与数值评分。
3. 保持 24h 与全量口径规则一致。

## 2. 计划范围（Plan）

1. 新增 24h one-hot adequacy level 指标。
2. 增加单测覆盖该指标输出。
3. 更新版本与文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_adequacy_level_24h{level=low|medium|high}`
2. 逻辑增强：
   - 回测质量快照中新增 `return_sample_adequacy_levels_onehot_24h`。
   - 与全量口径保持同一阈值与等级划分规则。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_backtest_return_sample_24h_adequacy_level`
4. 版本升级：
   - `0.3.41-m3-global-metrics-recent-adequacy-level`

## 4. 未完成项（Not Done）

1. 暂未输出 7d 窗口的 adequacy level 指标。
2. 暂未提供按窗口选择的统一查询参数（当前固定 24h）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代56-M3-近24小时样本充分性等级.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k sample_24h_adequacy_level`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k "sample_24h_adequacy_level or sample_24h_coverage_and_score"`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（24h one-hot adequacy level 指标可用）

## 7. 风险与问题

1. 风险描述：24h 样本窗口较短，等级可能短期波动。
2. 影响范围：告警噪声概率提升。
3. 缓解措施：与 24h sample_size、threshold_met、coverage、score 联合判定。

## 8. 关键决策

1. 决策内容：24h adequacy level 与全量 level 使用相同分档规则。
2. 决策原因：减少口径差异，提高运维和业务解释一致性。
3. 影响模块：全局指标聚合、监控规则模板。

## 9. 下迭代计划

1. 评估接入 7d 样本窗口核心指标（sample_size / threshold_met）。
2. 输出样本质量联动告警模板文档（全量 + 24h）。
3. 评估 24h 指标在看板中的默认展示顺序与阈值建议。
