# 迭代开发记录

迭代编号：`迭代20`  
日期：`2026-02-16`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将策略绑定中的 `prompt_refs` 接入聊天 Prompt 选择流程。
2. 实现“策略优先、默认兜底”的 Prompt 路由规则。
3. 保持现有聊天与策略上下文能力向后兼容。

## 2. 计划范围（Plan）

1. 先补失败测试，验证策略 prompt 能覆盖默认 prompt。
2. 修改 `ChatService` 提示词解析逻辑，支持多候选 prompt。
3. 回归聊天、策略、全量后端单测并执行 CI。

## 3. 实际完成（Done）

1. 新增测试：
   - `test_chat_uses_strategy_bound_prompt_ref_when_available`
2. `ChatService` 完成 Prompt 路由升级：
   - 先读取策略上下文中的 `prompt_refs`
   - 从 `prompt_refs` 解析候选 `prompt_id`
   - 按顺序尝试渲染；不可用时回退到 `prompt.chat.reply`
   - 最终保留 builtin prompt 兜底
3. 应用版本升级：
   - `0.3.5-m3-strategy-prompt-routing`

## 4. 未完成项（Not Done）

1. 暂未将 `prompt_refs` 的显式版本号（`@x`）作为强约束执行校验。
2. 暂未实现按策略类型/风险等级动态选择多个 Prompt 模板的权重策略。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/services/chat_service.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_strategy_context_injection.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-16-迭代20-M3-策略驱动Prompt路由.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_strategy_context_injection.py -k strategy_bound_prompt_ref`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_chat_prompt_binding.py refactor/backend/tests/unit/test_strategy_context_injection.py refactor/backend/tests/unit/test_chat_service.py`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest refactor/backend/tests/unit --maxfail=1`
   - `cd refactor/backend && ./scripts/ci.sh`
2. 结果摘要：
   - 新增定向测试：`1 passed`
   - 相关回归测试：通过
   - 后端单测：`43 passed`
   - CI：通过
3. 是否达到验收标准：
   - 达到（策略绑定已可驱动 Prompt 选择）

## 7. 风险与问题

1. 风险描述：当前 `prompt_refs` 仅解析 `prompt_id`，未按绑定中声明的版本精确锁定。
2. 影响范围：策略与提示词版本一致性治理。
3. 缓解措施：下一阶段增加“版本锁定模式”与“版本不可用回退策略”配置。

## 8. 关键决策

1. 决策内容：MVP 阶段优先采用“可用优先”路由，不阻断聊天主链路。
2. 决策原因：先保证策略驱动能力落地，再逐步增加版本治理强度。
3. 影响模块：Strategy Binding、Prompt Center、Chat Service。

## 9. 下迭代计划

1. 增加 `prompt_refs` 版本锁定与校验机制（严格模式/宽松模式）。
2. 将策略 Prompt 选择过程写入结构化审计日志，便于追踪。
3. 评估将同一机制扩展到分析链路（analysis prompts）。
