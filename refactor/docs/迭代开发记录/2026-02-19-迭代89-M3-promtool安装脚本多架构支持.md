# 迭代开发记录

迭代编号：`迭代89`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 为 `install-promtool.sh` 增加多架构自动识别能力。
2. 在不影响现有 checksum/版本锁定的前提下支持 `linux-amd64` 和 `linux-arm64`。
3. 对未知架构进行显式失败，避免静默错误。

## 2. 计划范围（Plan）

1. 先补失败测试，约束脚本需包含多架构识别分支。
2. 修改安装脚本实现自动识别逻辑。
3. 同步版本和文档。

## 3. 实际完成（Done）

1. 测试增强：
   - 新增 `test_promtool_installer_script_supports_multi_arch_auto_detection`。
2. 脚本升级：
   - `refactor/backend/scripts/install-promtool.sh`
   - 默认根据 `uname -m` 自动映射：
     - `x86_64`/`amd64` -> `linux-amd64`
     - `aarch64`/`arm64` -> `linux-arm64`
   - 对不支持架构直接报错并退出。
3. 版本升级：
   - `0.3.74-m3-promtool-installer-multi-arch`

## 4. 未完成项（Not Done）

1. 暂未增加对非 Linux 平台 artifact 的自动映射。
2. 暂未引入脚本级端到端下载 smoke（当前通过文本断言 + CI 流程间接覆盖）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/scripts/install-promtool.sh`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代89-M3-promtool安装脚本多架构支持.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（先失败后通过）
   - CI 脚本：通过
3. 是否达到验收标准：
   - 达到（多架构自动识别生效，未知架构显式失败）

## 7. 风险与问题

1. 风险描述：未来若新增 runner 架构，需补充映射分支。
2. 影响范围：CI 环境兼容性。
3. 缓解措施：后续将平台映射表配置化并补回归测试。

## 8. 关键决策

1. 决策内容：默认自动识别架构，同时保留 `PROMTOOL_PLATFORM` 显式覆盖能力。
2. 决策原因：兼顾“开箱可用”和“可控覆盖”。
3. 影响模块：promtool 安装脚本、CI 跨架构兼容性。

## 9. 下迭代计划

1. 评估并补充非 Linux 平台映射策略（按需）。
2. 为脚本补充更细粒度的错误码和日志分类。
3. 持续推进 M3 治理链路稳定性优化。
