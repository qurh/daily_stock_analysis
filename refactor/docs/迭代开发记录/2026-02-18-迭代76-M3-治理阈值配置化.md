# 迭代开发记录

迭代编号：`迭代76`  
日期：`2026-02-18`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将阈值治理等级的 warn/critical 比例阈值配置化。
2. 让治理等级在不同环境可调，而不是固定常量。
3. 保持现有指标名称兼容，并提升配置可观测性。

## 2. 计划范围（Plan）

1. 新增 settings 失败测试，覆盖治理阈值 env 读取与归一化。
2. 新增 metrics 失败测试，验证 env 覆盖后治理等级变化。
3. 实现配置读取、归一化与指标导出。
4. 同步版本与文档。

## 3. 实际完成（Done）

1. 新增配置项：
   - `BACKTEST_MULTI_WINDOW_ALERT_THRESHOLD_GOVERNANCE_WARN_RATIO`
   - `BACKTEST_MULTI_WINDOW_ALERT_THRESHOLD_GOVERNANCE_CRITICAL_RATIO`
2. 配置规则：
   - 两项阈值限制在 `0.0-1.0`
   - `critical_ratio` 自动提升到不小于 `warn_ratio`
3. 新增指标：
   - `refactor_backtest_records_return_sample_multi_window_alert_threshold_governance_warn_ratio`
   - `refactor_backtest_records_return_sample_multi_window_alert_threshold_governance_critical_ratio`
4. 测试增强：
   - `test_settings_reads_non_prefixed_env_vars`
   - `test_settings_normalizes_multi_window_alert_threshold_relationship`
   - `test_global_metrics_endpoint_threshold_governance_levels_support_env_ratio_overrides`
   - 按 TDD 验证新增测试先失败后通过。
5. 版本升级：
   - `0.3.61-m3-governance-threshold-ratio-config`

## 4. 未完成项（Not Done）

1. 暂未将治理阈值告警规则（PrometheusRule）模板文件化。
2. 暂未引入运行时动态热更新治理阈值能力。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/core/settings.py`
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_settings_env_names.py`
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/.env.example`
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-18-迭代76-M3-治理阈值配置化.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_settings_env_names.py -q`
   - `cd refactor/backend && pytest tests/unit/test_prompt_lock_audit.py -k "governance_levels_support_env_ratio_overrides or raw_normalized_mismatch_count" -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（新增用例先失败后通过）
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（治理阈值已配置化并可观测）

## 7. 风险与问题

1. 风险描述：治理阈值配置过低可能导致告警过于频繁。
2. 影响范围：告警噪声与运营负担。
3. 缓解措施：保留默认阈值 `0.25/0.5`，后续补告警模板与推荐策略。

## 8. 关键决策

1. 决策内容：先实现 env 配置化，不引入复杂的运行时配置中心。
2. 决策原因：最小改动满足当前阶段“可调 + 可验证”需求。
3. 影响模块：settings、metrics 导出与告警策略基线。

## 9. 下迭代计划

1. 产出 Prometheus 告警规则模板（warn/critical）并补文档示例。
2. 为治理阈值增加异常配置防护文档（推荐区间与调参建议）。
3. 评估是否需要把治理阈值纳入 chatbot 可控配置接口。
