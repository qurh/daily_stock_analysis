# 迭代开发记录

迭代编号：`迭代60`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 在现有多窗口观测基础上补齐 30d 样本门槛指标。
2. 支持按月尺度评估样本充足性。
3. 保持与全量/24h/7d 门槛规则一致。

## 2. 计划范围（Plan）

1. 新增 30d 样本量指标。
2. 新增 30d 门槛达标指标。
3. 补充测试与文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_size_30d`
   - `refactor_backtest_records_return_sample_size_threshold_met_30d`
2. 统计逻辑增强：
   - 回测样本快照新增 30d 时间窗口计数。
   - 30d 门槛达标复用统一充分性计算逻辑。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_backtest_return_sample_30d_threshold`
4. 版本升级：
   - `0.3.45-m3-global-metrics-30d-sample-threshold`

## 4. 未完成项（Not Done）

1. 暂未输出 30d 覆盖率、评分和等级指标。
2. 暂未提供可配置窗口参数。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代60-M3-近30天样本门槛指标.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k sample_30d_threshold`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k "sample_30d_threshold or sample_7d_adequacy_level"`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（30d 样本门槛指标可用）

## 7. 风险与问题

1. 风险描述：30d 窗口在样本不足时对趋势变化不敏感。
2. 影响范围：月度告警可能滞后。
3. 缓解措施：与 24h/7d 指标联合使用，形成快慢结合判断。

## 8. 关键决策

1. 决策内容：先交付 30d 门槛核心指标，不一次引入全部派生指标。
2. 决策原因：控制复杂度并保持迭代节奏。
3. 影响模块：全局 metrics 聚合、告警策略设计。

## 9. 下迭代计划

1. 评估补齐 30d 覆盖率与评分。
2. 评估是否补齐 30d one-hot 等级。
3. 输出多窗口样本质量联动告警模板文档。
