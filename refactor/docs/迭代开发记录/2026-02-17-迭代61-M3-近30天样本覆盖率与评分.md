# 迭代开发记录

迭代编号：`迭代61`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 在 30d 样本门槛基础上补齐覆盖率与评分指标。
2. 完成全量/24h/7d/30d 四窗口评分体系对齐。
3. 保持指标口径一致与低复杂度实现。

## 2. 计划范围（Plan）

1. 新增 30d coverage ratio 指标。
2. 新增 30d adequacy score 指标。
3. 新增测试并同步文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_coverage_ratio_pct_30d`
   - `refactor_backtest_records_return_sample_adequacy_score_30d`
2. 统计逻辑增强：
   - 基于 30d 窗口复用统一充分性计算函数输出 coverage/score。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_backtest_return_sample_30d_coverage_and_score`
4. 版本升级：
   - `0.3.46-m3-global-metrics-30d-sample-coverage-score`

## 4. 未完成项（Not Done）

1. 暂未输出 30d one-hot adequacy level。
2. 暂未输出窗口参数化 API。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代61-M3-近30天样本覆盖率与评分.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k sample_30d_coverage_and_score`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k "sample_30d_coverage_and_score or sample_30d_threshold"`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（30d 覆盖率与评分指标可用）

## 7. 风险与问题

1. 风险描述：30d 评分在低频策略场景下可能长期维持低位。
2. 影响范围：月度样本质量告警可能高频触发。
3. 缓解措施：结合 30d sample_size_threshold_met 一起使用。

## 8. 关键决策

1. 决策内容：30d 指标先补 coverage/score，不在同迭代引入更多衍生项。
2. 决策原因：控制迭代粒度，保持验证成本可控。
3. 影响模块：metrics 聚合与告警策略。

## 9. 下迭代计划

1. 评估补齐 30d one-hot adequacy level。
2. 输出多窗口联动告警模板文档。
3. 评估窗口参数化方案。
