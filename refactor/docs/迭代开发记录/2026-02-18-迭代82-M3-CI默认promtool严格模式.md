# 迭代开发记录

迭代编号：`迭代82`  
日期：`2026-02-18`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 在 CI 环境中默认启用 promtool 严格校验。
2. 避免流水线遗漏 Prometheus 规则语法问题。
3. 保留本地环境的可覆盖能力。

## 2. 计划范围（Plan）

1. 先补失败测试，要求 `ci.sh` 包含 `CI` 与 `PROMTOOL_REQUIRED` 逻辑。
2. 在 `ci.sh` 加入 CI 默认 strict mode。
3. 同步版本与文档。

## 3. 实际完成（Done）

1. `ci.sh` 逻辑增强：
   - 当 `CI` 环境变量存在时，自动执行：
     - `PROMTOOL_REQUIRED=${PROMTOOL_REQUIRED:-1}`
2. 测试增强：
   - 扩展 `test_ci_script_invokes_prometheus_rules_check`，断言 `ci.sh` 含 `CI` 与 `PROMTOOL_REQUIRED`。
3. 版本升级：
   - `0.3.67-m3-ci-default-promtool-strict`

## 4. 未完成项（Not Done）

1. 暂未在具体 CI 平台配置中显式声明 promtool 安装步骤。
2. 暂未新增 CI 失败时的可观测输出聚合。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/scripts/ci.sh`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-18-迭代82-M3-CI默认promtool严格模式.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（先失败后通过）
   - 全量单测：通过
   - CI：通过（本地无 CI 环境变量时保持兼容）
3. 是否达到验收标准：
   - 达到（CI 默认 strict mode 生效）

## 7. 风险与问题

1. 风险描述：CI 基础镜像若未安装 promtool，将触发失败。
2. 影响范围：流水线可用性。
3. 缓解措施：在 CI 文档中补充 promtool 安装前置步骤。

## 8. 关键决策

1. 决策内容：在脚本层面内置 CI 默认 strict，优先保障质量门禁。
2. 决策原因：避免依赖手工配置导致策略失效。
3. 影响模块：CI 脚本行为与规则校验策略。

## 9. 下迭代计划

1. 补充 CI 平台级示例配置（安装 promtool + 运行 ci.sh）。
2. 为 promtool 校验输出增加简要统计摘要。
3. 评估将规则文件校验与单元测试分阶段执行。
