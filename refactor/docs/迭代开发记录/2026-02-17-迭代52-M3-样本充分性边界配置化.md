# 迭代开发记录

迭代编号：`迭代52`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将样本充分性 `medium` 等级边界从硬编码改为可配置。
2. 提升不同策略场景下样本等级判读的可调优性。
3. 保持指标与接口兼容。

## 2. 计划范围（Plan）

1. 新增配置项 `BACKTEST_RETURN_SAMPLE_MEDIUM_COVERAGE_PCT`。
2. 将 adequacy level 计算逻辑改为读取配置值。
3. 补充指标输出与测试验证。

## 3. 实际完成（Done）

1. 新增配置项：
   - `BACKTEST_RETURN_SAMPLE_MEDIUM_COVERAGE_PCT`（默认 `50`）
2. 新增指标：
   - `refactor_backtest_records_return_sample_medium_coverage_threshold_pct`
3. 逻辑变更：
   - `medium` 判断边界由固定 `50%` 调整为配置值。
   - 对配置值做范围保护（`0~100`）。
4. 测试增强：
   - 新增 `test_global_metrics_endpoint_sample_adequacy_level_supports_medium_threshold_override`
   - 更新 `test_settings_reads_non_prefixed_env_vars` 增加配置断言。
5. 版本升级：
   - `0.3.37-m3-global-metrics-adequacy-threshold-config`

## 4. 未完成项（Not Done）

1. 暂未开放 `low/medium/high` 全部边界参数（当前仅 medium 覆盖率边界可配）。
2. 暂未接入配置热更新（需重启进程生效）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/core/settings.py`
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
   - `refactor/backend/tests/unit/test_settings_env_names.py`
3. 文档路径：
   - `refactor/backend/.env.example`
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代52-M3-样本充分性边界配置化.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k medium_threshold_override`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_settings_env_names.py`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（样本充分性等级边界支持配置化）

## 7. 风险与问题

1. 风险描述：配置值过高/过低可能导致等级分布失真。
2. 影响范围：告警噪声或漏报风险。
3. 缓解措施：结合历史分布和回测样本规模进行阈值校准。

## 8. 关键决策

1. 决策内容：先仅开放 medium 覆盖率边界配置，避免参数爆炸。
2. 决策原因：控制复杂度，优先满足主要调优需求。
3. 影响模块：配置加载、指标聚合、告警规则配置。

## 9. 下迭代计划

1. 评估补充样本等级分布趋势指标（按时间窗口）。
2. 输出样本相关指标联动告警模板文档。
3. 评估是否需要将 low/high 边界进一步配置化。
