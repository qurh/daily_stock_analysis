# 迭代开发记录

迭代编号：`迭代75`  
日期：`2026-02-18`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 基于阈值偏差 count/ratio/total 输出可直接消费的治理告警等级。
2. 提供 one-hot 等级与数值化 score，便于 Prometheus 告警与看板展示。
3. 保持现有阈值偏差指标兼容。

## 2. 计划范围（Plan）

1. 先新增治理等级断言并按 TDD 实现。
2. 在 metrics 快照中增加治理等级计算逻辑。
3. 在 `/api/v2/metrics` 导出治理等级指标。
4. 同步版本与文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_multi_window_alert_threshold_governance_level{level=none|warn|critical}`
   - `refactor_backtest_records_return_sample_multi_window_alert_threshold_governance_level_score`
2. 逻辑增强：
   - 新增阈值治理等级计算：
     - `none`：ratio < 0.25
     - `warn`：0.25 <= ratio < 0.5
     - `critical`：ratio >= 0.5
   - score 采用 `none=0.0/warn=0.5/critical=1.0`。
3. 测试增强：
   - 在 `test_global_metrics_endpoint_includes_raw_normalized_mismatch_count` 增加治理等级与 score 断言。
   - 按 TDD 验证新增断言先失败后通过。
4. 版本升级：
   - `0.3.60-m3-global-metrics-threshold-governance-level`

## 4. 未完成项（Not Done）

1. 暂未将治理等级阈值（0.25/0.5）外置为配置项。
2. 暂未产出 Prometheus 告警规则模板文件。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-18-迭代75-M3-阈值治理告警等级指标.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_prompt_lock_audit.py -k "raw_normalized_mismatch_count" -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（新增断言先失败后通过）
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（阈值治理等级可观测）

## 7. 风险与问题

1. 风险描述：治理等级阈值为固定常量，不同环境可能需要不同灵敏度。
2. 影响范围：告警触发频率与噪声控制。
3. 缓解措施：下一迭代将阈值参数化并产出默认告警模板。

## 8. 关键决策

1. 决策内容：先以内置阈值交付统一治理等级语义，再做配置化。
2. 决策原因：减少改动面，先验证指标消费价值。
3. 影响模块：metrics 快照、Prometheus 指标导出、告警策略设计。

## 9. 下迭代计划

1. 产出 Prometheus 告警规则模板（warn/critical 两级）。
2. 将治理等级阈值抽为配置项并补充设置解析测试。
3. 在 docs 补充告警规则使用示例。
