# 2026-03-04 迭代271 - 前后端联调烟测

迭代编号：`迭代271`  
日期：`2026-03-04`  
负责人：`Codex`

---

## 1. 本迭代目标

1. 验证当前 M4 前后端是否具备联调测试条件。
2. 按前端五个页面实际调用链路执行后端 API 烟测。
3. 输出通过项与阻塞项，明确下一步动作。

## 2. 测试环境

1. 后端启动参数：
   - `LLM_PROVIDER=mock-llm`
   - `DATABASE_URL=sqlite:////tmp/refactor_integration_<timestamp>.sqlite3`
   - `CHROMA_PATH=/tmp/refactor_integration_chroma_<timestamp>`
2. 前端验证：
   - `npm run build`
   - `npm run test -- --run`

## 3. 联调清单与结果

### 3.1 前端工程可运行性

1. `npm run build`：通过
2. `npm run test -- --run`：通过（`4` files / `7` tests）

### 3.2 后端 API 联调（按页面）

1. Health
   - `GET /api/v2/health`：`200`
2. Knowledge 页面
   - `POST /knowledge/documents/upload`：`201`
   - `POST /knowledge/documents/{doc_id}/optimize`：`200`
   - `POST /knowledge/documents/{doc_id}/ingest`：`202`
   - `GET /knowledge/documents/{doc_id}`：`200`
   - `GET /knowledge/chunks/search`：`200`
   - `DELETE /knowledge/documents/{doc_id}`：`200`
3. Chat 页面
   - `POST /chat/sessions`：`201`
   - `POST /chat/sessions/{session_id}/messages`：`200`
   - `GET /chat/sessions/{session_id}/messages`：`200`
4. Workflow 页面
   - `POST /workflows/executions`：`202`
   - `GET /workflows/executions/{execution_id}`：`200`
   - `POST /workflows/executions/{execution_id}/cancel`：`200`
5. Backtest 页面
   - `POST /backtest/jobs`：`202`
   - `GET /backtest/jobs/{job_id}`：`200`
   - `GET /backtest/results`：`200`
   - `GET /backtest/performance`：`200`
6. Strategy 页面
   - `POST /strategy/cognition/distill`：`201`
   - `POST /strategy/cognition/{memo_id}/review`：`200`
   - `POST /strategy/extract`：`201`
   - `GET /strategy/versions`：`200`
   - `POST /strategy/{strategy_id}/publish`：`409`
   - 409 详情：`STR-GATE-005: strategy did not pass backtest gate`

## 4. 结论

1. 当前项目已具备前后端联调测试条件，核心链路可跑通。
2. 唯一阻塞点位于策略发布业务门禁（非接口不可用）：
   - 发布前必须满足回测门禁（样本/胜率等条件）。
3. 这属于业务规则校验命中，前端需正确展示错误并引导补齐前置条件。

## 5. 下一步建议

1. 在 Strategy 页增加“发布前置条件检查”提示（展示 backtest gate 当前状态）。
2. 增加一条“可发布”正向联调样本数据路径（确保 `publish=200` + `bind` + `rollback` 全链路可验证）。
3. 将该联调脚本固化为可重复执行的集成测试脚本（CI 可选）。
