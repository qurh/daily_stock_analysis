# 迭代开发记录

迭代编号：`迭代65`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 在 multi-window alert level one-hot 基础上补齐数值化评分。
2. 支持告警阈值系统直接基于单指标做判断。
3. 保持最小变更并完成回归验证。

## 2. 计划范围（Plan）

1. 新增 alert level score 指标。
2. 新增对应单测，按 TDD 流程实现。
3. 更新版本号与文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_multi_window_alert_level_score`
2. 逻辑增强：
   - 新增等级评分映射：
     - `none -> 0.0`
     - `warn -> 0.5`
     - `critical -> 1.0`
   - 评分由现有 multi-window alert level one-hot 推导，保持分级语义一致。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_backtest_return_sample_multi_window_alert_level_score`
   - 按 TDD 验证新增测试先失败后通过。
4. 版本升级：
   - `0.3.50-m3-global-metrics-multi-window-alert-level-score`

## 4. 未完成项（Not Done）

1. 暂未引入 score 阈值配置项（目前固定 0.0/0.5/1.0）。
2. 暂未提供策略维度或市场维度拆分 score。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代65-M3-多窗口样本联动告警等级评分.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_prompt_lock_audit.py -k "multi_window_alert_level_score" -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（新增用例先失败后通过）
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（multi-window alert level score 指标可用）

## 7. 风险与问题

1. 风险描述：离散评分映射在边界场景可能损失细粒度信息。
2. 影响范围：告警系统灵敏度调优。
3. 缓解措施：后续可引入连续评分或可配置权重方案。

## 8. 关键决策

1. 决策内容：采用固定分段评分（0.0/0.5/1.0），与 one-hot 等级严格一致。
2. 决策原因：便于监控系统接入并保持解释一致性。
3. 影响模块：metrics 聚合、告警规则配置。

## 9. 下迭代计划

1. 评估将 multi-window alert 分级阈值配置化。
2. 评估新增 `warn_windows_total` 等辅助指标。
3. 输出 Prometheus 告警规则模板文档草案。
