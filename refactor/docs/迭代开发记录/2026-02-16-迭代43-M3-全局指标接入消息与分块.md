# 迭代开发记录

迭代编号：`迭代43`  
日期：`2026-02-16`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将会话消息与知识分块指标接入全局指标端点。
2. 补齐知识学习与对话链路的核心可观测性覆盖。
3. 保持 `/api/v2/metrics` Prometheus 文本格式一致性。

## 2. 计划范围（Plan）

1. 新增 conversation_messages 总量指标。
2. 新增 conversation_messages 按 role 分组指标。
3. 新增 knowledge_chunks 总量与 token 累计指标。
4. 补齐测试并同步版本与文档。

## 3. 实际完成（Done）

1. 指标聚合增强：
   - `refactor_conversation_messages_total`
   - `refactor_conversation_messages_by_role_total{role=...}`
   - `refactor_knowledge_chunks_total`
   - `refactor_knowledge_chunks_token_count_total`
2. 全局指标输出内容：
   - build info
   - prompt lock overview 指标
   - backtest/optimization/analysis/workflow 指标
   - knowledge/chat/memory 指标
   - message/chunk 指标
3. 新增并通过测试：
   - `test_global_metrics_endpoint_includes_message_and_chunk_metrics`
4. 应用版本升级：
   - `0.3.28-m3-global-metrics-message-chunk`

## 4. 未完成项（Not Done）

1. 暂未引入消息与分块的时间窗速率指标（需结合时序系统计算）。
2. 暂未引入向量索引一致性与召回质量类指标。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-16-迭代43-M3-全局指标接入消息与分块.md`

## 6. 验证记录

1. 执行命令：
   - `pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k message_and_chunk_metrics`
   - `pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k global_metrics_endpoint`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（全局指标新增消息与分块覆盖）

## 7. 风险与问题

1. 风险描述：当前为库存量与累计量指标，异常峰值定位依赖外部时序告警规则。
2. 影响范围：单次抓取不直接体现消息吞吐趋势。
3. 缓解措施：在 Prometheus/Grafana 侧配置 rate、increase 与分角色阈值告警。

## 8. 关键决策

1. 决策内容：会话消息统计同时提供总量和 role 分组，知识分块同时提供数量和 token 累计。
2. 决策原因：兼顾规模监控、角色分布观察与语料体量基线评估。
3. 影响模块：全局指标聚合逻辑与监控面板配置。

## 9. 下迭代计划

1. 评估新增回测/优化产物质量相关指标（收益分布、参数搜索覆盖率）。
2. 评估 prompt-lock 治理指标与核心业务指标的联动告警模板。
3. 整理指标命名与标签规范文档（包含 cardinality 控制准则）。
