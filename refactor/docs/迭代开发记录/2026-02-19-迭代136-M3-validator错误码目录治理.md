# 迭代开发记录

迭代编号：`迭代136`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 新增 validator 错误码目录文件，集中管理可对外暴露的错误码。
2. 用测试约束“脚本错误码必须被目录覆盖”，避免文档漂移。

## 2. 计划范围（Plan）

1. 先补失败测试定义目录存在性与覆盖约束。
2. 新增 `validator-error-codes.json` 并补齐错误码条目。
3. 同步 README / CHANGELOG / 版本号。

## 3. 实际完成（Done）

1. TDD Red：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - 新增失败场景：
     - `config/validator-error-codes.json` 必须存在并含 `summary_schema` 与 `summary_contract` 分组
     - 目录必须覆盖两个 validator 脚本中的全部字面量错误码
2. TDD Green：
   - 新增目录文件：
     - `refactor/backend/config/validator-error-codes.json`
   - 覆盖两类 validator 的错误码分组与描述：
     - `summary_schema.*`
     - `summary_contract.*`
3. 文档与版本：
   - `refactor/backend/README.md` 增加错误码目录路径说明。
   - `refactor/docs/CHANGELOG.md` 新增 `0.3.121-m3-validator-error-code-catalog`。
   - `refactor/backend/src/app/main.py` 版本升级：`0.3.121-m3-validator-error-code-catalog`。

## 4. 未完成项（Not Done）

1. 暂未增加“新增错误码必须包含示例 remediation”的文档约束。
2. 暂未将动态错误码（非字面量）自动纳入覆盖检查。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/config/validator-error-codes.json`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代136-M3-validator错误码目录治理.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "validator_error_code_catalog" -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 错误码目录相关新增用例通过
   - 后端全量 CI 通过
3. 是否达到验收标准：
   - 达到（错误码目录已建立并受测试保护）

## 7. 风险与问题

1. 风险描述：当前覆盖策略只抓取字面量 `code="..."`，对动态拼接错误码覆盖不足。
2. 影响范围：错误码目录完整性。
3. 缓解措施：后续补充动态错误码白名单或显式清单校验。

## 8. 关键决策

1. 决策内容：先用轻量测试守住目录存在与静态覆盖，再迭代增强动态覆盖能力。
2. 决策原因：快速落地且风险可控。
3. 影响模块：错误码治理与自动化一致性。

## 9. 下迭代计划

1. 增加动态错误码显式声明机制。
2. 将错误码目录扩展为可供前端/告警系统消费的接口文档。
3. 评估生成错误码 Markdown 手册并加入发布产物。
