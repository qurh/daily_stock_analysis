# 迭代开发记录

迭代编号：`迭代62`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 补齐 30d 样本充分性 one-hot 等级观测能力。
2. 完成全量/24h/7d/30d 四窗口等级指标对齐。
3. 保持最小改动并通过现有回归验证。

## 2. 计划范围（Plan）

1. 新增 30d one-hot adequacy level 指标。
2. 新增对应单测并走 TDD 红绿流程。
3. 更新版本号与文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_adequacy_level_30d{level=low|medium|high}`
2. 逻辑增强：
   - 回测质量快照新增 `return_sample_adequacy_levels_onehot_30d`。
   - 全局指标导出新增 30d one-hot 等级输出（low/medium/high）。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_backtest_return_sample_30d_adequacy_level`
   - 先验证失败，再实现通过（TDD）。
4. 版本升级：
   - `0.3.47-m3-global-metrics-30d-adequacy-level`

## 4. 未完成项（Not Done）

1. 暂未提供自定义时间窗口参数化能力（仍固定全量/24h/7d/30d）。
2. 暂未新增窗口联动告警策略模板接口。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代62-M3-近30天样本充分性等级.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_prompt_lock_audit.py -k "30d_adequacy_level" -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（新增用例先失败后通过）
   - 全量单测：通过（`101 passed`）
   - CI：通过
3. 是否达到验收标准：
   - 达到（30d one-hot adequacy level 指标可用）

## 7. 风险与问题

1. 风险描述：多窗口指标持续增长可能增加告警面板维护成本。
2. 影响范围：监控规则维护与解释成本。
3. 缓解措施：后续整理窗口聚合视图与统一告警模板。

## 8. 关键决策

1. 决策内容：延续“小步快跑”策略，30d 等级指标单独成迭代。
2. 决策原因：降低单次变更风险，保证可快速回归与定位问题。
3. 影响模块：metrics 聚合、监控告警配置。

## 9. 下迭代计划

1. 评估补齐 30d 之外的跨窗口联动充分性指标。
2. 评估窗口参数化与查询性能影响。
3. 输出多窗口样本质量告警模板文档。
