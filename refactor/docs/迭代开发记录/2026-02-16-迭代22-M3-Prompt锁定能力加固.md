# 迭代开发记录

迭代编号：`迭代22`  
日期：`2026-02-16`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 为 strict 模式补齐结构化错误明细，提升可观测性与可排障性。
2. 将 `prompt_refs` 锁定策略从聊天链路扩展到分析链路。
3. 保持“全局默认 + 绑定覆盖”一致行为。

## 2. 计划范围（Plan）

1. 新增 strict 错误结构化断言测试。
2. 新增 analysis strict/lenient 锁定行为测试。
3. 抽取共享 prompt 路由能力并接入 chat + analysis。

## 3. 实际完成（Done）

1. 抽取共享模块 `prompt_routing.py`：
   - `PromptLockError`
   - `parse_prompt_ref / normalize_prompt_refs / normalize_lock_mode`
   - `resolve_binding_prompt`
2. 聊天链路增强：
   - strict 失败返回结构化 `409` 明细
3. 分析链路接入锁定策略：
   - 绑定 prompt 解析遵循 strict/lenient
   - 分析结果 `meta` 新增 `prompt_ref`
   - strict 失败时任务置为 `failed`，并在 job 结果中持久化结构化错误
4. 新增并通过测试：
   - chat strict 结构化错误明细
   - analysis strict 失败
   - analysis 绑定级 lenient 覆盖全局 strict
5. 应用版本升级：
   - `0.3.7-m3-prompt-lock-hardening`

## 4. 未完成项（Not Done）

1. 暂未补 strict 失败的细粒度分类统计接口（按失败原因聚合）。
2. 暂未把同样错误结构化扩展到更多流程节点（如 workflow 子节点级）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/services/prompt_routing.py`
   - `refactor/backend/src/app/services/chat_service.py`
   - `refactor/backend/src/app/services/analysis_service.py`
   - `refactor/backend/src/app/services/prompt_service.py`
   - `refactor/backend/src/app/api/routes/chat.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_strategy_context_injection.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-16-迭代22-M3-Prompt锁定能力加固.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_strategy_context_injection.py -k "strict_prompt_lock_mode_rejects_missing_bound_version or analysis_binding_lenient_overrides_global_strict_mode"`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_chat_service.py refactor/backend/tests/unit/test_chat_prompt_binding.py refactor/backend/tests/unit/test_analysis_jobs.py refactor/backend/tests/unit/test_settings_env_names.py`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest refactor/backend/tests/unit --maxfail=1`
   - `cd refactor/backend && ./scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 后端单测：`47 passed`
   - CI：通过
3. 是否达到验收标准：
   - 达到（prompt 锁定能力在 chat + analysis 两条主链路可用）

## 7. 风险与问题

1. 风险描述：分析链路目前只做 prompt 解析与引用记录，未深入到多阶段节点级 Prompt 治理。
2. 影响范围：复杂分析编排场景的精细化可控能力。
3. 缓解措施：下一阶段在 workflow 子节点层引入独立 prompt 绑定策略。

## 8. 关键决策

1. 决策内容：strict 失败统一采用结构化错误模型，便于前端和审计系统消费。
2. 决策原因：先统一错误契约，再扩展可观测与统计能力。
3. 影响模块：Prompt 路由、Chat、Analysis、API 错误映射。

## 9. 下迭代计划

1. 增加 strict 失败原因聚合统计与审计查询接口。
2. 将 prompt 版本锁定扩展到 workflow 子节点级执行。
3. 完成 M3 端到端演练自动化用例（提案->回测->审批->生效->回滚）。
