# 迭代开发记录

迭代编号：`迭代87`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 为 backend CI 中的 promtool 下载增加 `checksum` 校验。
2. 强化 CI 供应链完整性，避免下载内容被篡改或损坏。
3. 通过测试锁定校验步骤，防止后续回退。

## 2. 计划范围（Plan）

1. 先补失败测试，要求 workflow 必须包含固定 SHA256 与 `sha256sum -c -`。
2. 更新 active/example workflow 的 promtool 安装步骤。
3. 同步版本与文档。

## 3. 实际完成（Done）

1. 测试增强：
   - 在 `test_ci_prometheus_rules_check.py` 增加 SHA256 校验断言。
   - 校验字段：
     - `PROMTOOL_SHA256=7f31c5d...`
     - `sha256sum -c -`
2. Workflow 升级：
   - `.github/workflows/refactor-backend-ci.yml`
   - `refactor/backend/ci/github-actions/refactor-backend-ci.example.yml`
   - 新增下载后 SHA256 校验步骤。
3. 版本升级：
   - `0.3.72-m3-promtool-checksum-verification`

## 4. 未完成项（Not Done）

1. 暂未将 promtool 安装逻辑抽取为复用脚本。
2. 暂未增加 checksum 来源自动更新机制。

## 5. 代码与文档变更

1. 代码路径：
   - `.github/workflows/refactor-backend-ci.yml`
   - `refactor/backend/ci/github-actions/refactor-backend-ci.example.yml`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代87-M3-promtool校验和验证.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（先失败后通过）
   - CI 脚本：通过
3. 是否达到验收标准：
   - 达到（promtool 下载链路具备完整性校验，且有测试约束）

## 7. 风险与问题

1. 风险描述：固定版本与固定 checksum 需要人工维护升级。
2. 影响范围：工具链版本更新节奏与维护成本。
3. 缓解措施：后续引入版本升级脚本和回归检查清单。

## 8. 关键决策

1. 决策内容：在 workflow 中直接做 checksum 校验，而非仅依赖下载源可信度。
2. 决策原因：最小改动提升供应链安全，且实现简单可验证。
3. 影响模块：GitHub Actions workflow、CI 可靠性治理。

## 9. 下迭代计划

1. 抽取 promtool 安装与校验为可复用脚本（减少 workflow 重复）。
2. 评估固定版本升级流程与自动化校验策略。
3. 继续推进 M3 治理可观测性增强。
