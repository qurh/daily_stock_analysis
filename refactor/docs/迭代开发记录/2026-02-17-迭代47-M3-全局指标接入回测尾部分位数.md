# 迭代开发记录

迭代编号：`迭代47`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 在全局指标中补充回测收益尾部分位数观测（p95/p99）。
2. 强化收益分布的尾部风险识别能力。
3. 保持现有指标口径与输出格式兼容。

## 2. 计划范围（Plan）

1. 新增回测收益 `p95` 指标。
2. 新增回测收益 `p99` 指标。
3. 补充测试并同步版本和文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_pct_p95`
   - `refactor_backtest_records_return_pct_p99`
2. 统计逻辑增强：
   - 在现有线性插值分位数计算基础上，扩展输出 p95/p99。
3. 测试增强：
   - `test_global_metrics_endpoint_includes_backtest_return_quantiles_and_stddev` 补充 p95/p99 断言并通过。
4. 版本升级：
   - `0.3.32-m3-global-metrics-return-tail-quantiles`

## 4. 未完成项（Not Done）

1. 暂未引入按时间窗口的 rolling quantile 指标。
2. 暂未引入按策略版本维度的分位数（需先设计低基数标签方案）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代47-M3-全局指标接入回测尾部分位数.md`

## 6. 验证记录

1. 执行命令：
   - `pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k backtest_return_quantiles_and_stddev`
   - `pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k global_metrics_endpoint`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（全局指标覆盖 p95/p99 尾部收益分位数）

## 7. 风险与问题

1. 风险描述：极小样本下 p99 与最大值高度接近，解释需结合样本量。
2. 影响范围：尾部告警可能在样本不足时出现噪声。
3. 缓解措施：告警策略增加 sample_size 下限门槛。

## 8. 关键决策

1. 决策内容：不新增标签维度，仅新增全局分位数指标，控制指标基数。
2. 决策原因：优先补齐风险观测，同时保持 Prometheus 采集成本可控。
3. 影响模块：全局指标聚合逻辑、告警阈值配置。

## 9. 下迭代计划

1. 评估引入 trimmed mean / winsorized mean 以降低极端值影响。
2. 评估优化任务参数搜索覆盖率字段与指标设计。
3. 编制“样本量门槛 + 尾部分位数”告警建议模板。
