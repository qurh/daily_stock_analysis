# 迭代开发记录

迭代编号：`迭代63`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 补齐多窗口样本质量联动观测能力。
2. 将 24h/7d/30d 的样本充分性结果聚合为告警可直接使用的计数指标。
3. 在保持最小改动前提下完成可回归验证。

## 2. 计划范围（Plan）

1. 新增“未达门槛窗口数”指标。
2. 新增“low 充分性窗口数”指标。
3. 新增对应单测并更新版本和文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_threshold_unmet_windows_total`
   - `refactor_backtest_records_return_sample_low_adequacy_windows_total`
2. 逻辑增强：
   - 回测质量快照新增 24h/7d/30d 联动统计，输出门槛未满足窗口计数与 low 充分性窗口计数。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_backtest_return_sample_multi_window_alert_counts`
   - 按 TDD 流程验证新增测试先失败后通过。
4. 版本升级：
   - `0.3.48-m3-global-metrics-multi-window-alert-counts`

## 4. 未完成项（Not Done）

1. 暂未引入联动告警等级（warning/critical）one-hot 指标。
2. 暂未提供窗口集合可配置能力（当前固定 24h/7d/30d）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代63-M3-多窗口样本联动告警计数.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_prompt_lock_audit.py -k "multi_window_alert_counts" -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（新增用例先失败后通过）
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（多窗口联动告警计数指标可用）

## 7. 风险与问题

1. 风险描述：计数指标对阈值配置敏感，阈值过严时可能长期处于高告警。
2. 影响范围：监控告警策略与仪表盘解释。
3. 缓解措施：结合既有 coverage/score 指标共同判断并在后续提供模板阈值建议。

## 8. 关键决策

1. 决策内容：先输出联动计数，不在本迭代引入复杂等级策略。
2. 决策原因：控制迭代粒度并降低回归成本。
3. 影响模块：metrics 聚合与告警编排层。

## 9. 下迭代计划

1. 评估新增联动告警等级 one-hot（none/warn/critical）。
2. 评估窗口参数化与默认阈值配置方案。
3. 补充告警模板文档与示例规则。
