# 迭代开发记录

迭代编号：`迭代98`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 为 `PROMTOOL_VALIDATE_REMOTE_MODE=soft` 的降级路径补齐可观测性。
2. 输出可检索的 soft 降级指标日志，便于后续统计与告警。
3. 支持可选审计落盘，记录 soft 降级的关键上下文。

## 2. 计划范围（Plan）

1. 先补失败测试，约束 soft 模式下必须输出指标日志并支持审计落盘。
2. 增强 `validate-promtool-installer-config.sh`，输出结构化 soft 降级日志并写审计文件。
3. 同步版本、README、CHANGELOG 并完成回归验证。

## 3. 实际完成（Done）

1. 脚本增强：
   - `refactor/backend/scripts/validate-promtool-installer-config.sh`
   - 新增 `PROMTOOL_REMOTE_SOFT_AUDIT_FILE`（可选）。
   - 远端校验函数记录失败原因，并在 soft 降级时输出指标日志：
     - `metric remote_soft_fallback_total=1 mode=soft reason="..."`
   - 当配置审计文件路径时，追加写入 soft 降级审计记录。
2. 测试增强：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - 新增/增强用例：
     - soft 模式 mismatch 时输出指标日志。
     - soft 模式配置审计文件时写入审计记录。
3. 文档与版本：
   - `refactor/backend/src/app/main.py` 版本升级：`0.3.83-m3-promtool-soft-mode-audit`
   - `refactor/backend/README.md` 增加 soft 模式指标与审计配置说明。
   - `refactor/docs/CHANGELOG.md` 增加本次变更记录。

## 4. 未完成项（Not Done）

1. 暂未将 soft 降级指标接入 Prometheus 指标端点（当前为日志侧指标样式输出）。
2. 暂未对审计文件做轮转策略（当前按追加写入）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/scripts/validate-promtool-installer-config.sh`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代98-M3-soft门禁审计与指标.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（22 passed）
   - CI 脚本：通过
3. 是否达到验收标准：
   - 达到（soft 降级路径具备指标日志与可选审计落盘能力）

## 7. 风险与问题

1. 风险描述：审计文件若长期启用可能持续增长。
2. 影响范围：本地磁盘空间与日志治理。
3. 缓解措施：后续增加审计文件轮转/清理策略。

## 8. 关键决策

1. 决策内容：先用日志指标 + 可选文件审计实现最小可用可观测性。
2. 决策原因：低耦合、低成本、可快速落地并支持后续接入统一监控。
3. 影响模块：promtool 远端校验治理链路。

## 9. 下迭代计划

1. 评估将 soft 降级计数接入应用内 `/metrics` 指标。
2. 设计审计文件轮转与保留策略。
3. 持续推进 M3 稳定性与治理闭环。
