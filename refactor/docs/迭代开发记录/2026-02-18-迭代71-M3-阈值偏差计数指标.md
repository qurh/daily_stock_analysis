# 迭代开发记录

迭代编号：`迭代71`  
日期：`2026-02-18`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 新增 raw 与 normalized 阈值偏差的聚合指标。
2. 提供可直接用于治理告警的单一计数信号。
3. 在最小改动下保持现有行为兼容。

## 2. 计划范围（Plan）

1. 新增 mismatch count 指标测试并按 TDD 实现。
2. 在 metrics 快照中计算偏差计数。
3. 同步版本、文档、迭代记录。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_multi_window_alert_threshold_raw_normalized_mismatch_count`
2. 逻辑增强：
   - 对四个阈值维度（warn/critical × low/unmet）逐项比较 raw 与 normalized。
   - 汇总输出 mismatch count（范围 0-4）。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_raw_normalized_mismatch_count`
   - 按 TDD 验证新增测试先失败后通过。
4. 版本升级：
   - `0.3.56-m3-global-metrics-threshold-mismatch-count`

## 4. 未完成项（Not Done）

1. 暂未按维度输出 mismatch 比例或标签化明细。
2. 暂未提供 mismatch 历史趋势接口。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-18-迭代71-M3-阈值偏差计数指标.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_prompt_lock_audit.py -k "raw_normalized_mismatch_count" -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（新增用例先失败后通过）
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（阈值偏差计数可观测）

## 7. 风险与问题

1. 风险描述：单计数指标无法直接指出具体偏差维度。
2. 影响范围：告警定位效率。
3. 缓解措施：已有 raw/normalized/flags 可组合定位，后续可补标签化明细。

## 8. 关键决策

1. 决策内容：先输出聚合计数，再评估标签化维度细分。
2. 决策原因：控制指标基数，降低 Prometheus 规则复杂度。
3. 影响模块：metrics 导出与告警治理规则。

## 9. 下迭代计划

1. 评估输出 mismatch 维度细分指标（按 low/unmet 两类）。
2. 输出阈值治理告警模板（基于 mismatch count + normalization flags）。
3. 评估 mismatch 指标在 dashboard 的呈现规范。
