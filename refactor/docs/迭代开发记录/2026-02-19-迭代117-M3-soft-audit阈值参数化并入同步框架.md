# 迭代开发记录

迭代编号：`迭代117`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将 soft-audit 告警阈值（时长与严重级别）并入既有参数化同步框架。
2. 保证 strict gate 与 soft-audit 告警配置由同一配置源统一驱动。
3. 保持现有规则文件表达式不变，仅同步可调参数字段。

## 2. 计划范围（Plan）

1. 先补失败测试，要求配置中存在 soft-audit 参数字段。
2. 更新配置文件，补齐四个 profile 的 soft-audit 参数。
3. 扩展同步脚本，使其同步 soft-audit 告警的 `for` 与 `severity`。
4. 同步 README、CHANGELOG、版本号。

## 3. 实际完成（Done）

1. TDD Red：
   - `refactor/backend/tests/unit/test_strict_gate_alert_threshold_sync.py`
   - 新增 soft-audit 字段断言后失败（配置缺字段）。
2. TDD Green：
   - `refactor/backend/config/strict-gate-alert-thresholds.json`
     - 为 `default/dev/staging/prod` 新增 6 个 soft-audit 参数字段。
   - `refactor/backend/scripts/sync-strict-gate-alert-thresholds.py`
     - 新增 soft-audit 三条告警的 `for` 与 `severity` 同步逻辑。
3. 文档与版本：
   - `refactor/backend/README.md` 补充配置覆盖范围说明。
   - `refactor/docs/CHANGELOG.md` 新增 `0.3.102-m3-soft-audit-threshold-param-sync`。
   - `refactor/backend/src/app/main.py` 版本升级：`0.3.102-m3-soft-audit-threshold-param-sync`。

## 4. 未完成项（Not Done）

1. 暂未将 threshold governance 相关参数并入同一同步脚本。
2. 暂未增加 soft-audit 参数合法性（枚举/时长格式）专门校验测试。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/config/strict-gate-alert-thresholds.json`
   - `refactor/backend/scripts/sync-strict-gate-alert-thresholds.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_strict_gate_alert_threshold_sync.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代117-M3-soft-audit阈值参数化并入同步框架.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_strict_gate_alert_threshold_sync.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 新增配置字段测试通过
   - 全量 CI 通过（promtool 缺失按现有策略 skip）
3. 是否达到验收标准：
   - 达到（soft-audit 告警参数已统一纳入参数化同步）

## 7. 风险与问题

1. 风险描述：单配置文件承载告警参数变多，配置误改风险上升。
2. 影响范围：告警敏感度、告警噪声、运维响应。
3. 缓解措施：维持 CI `--check` 门禁，并后续补充配置 schema 校验。

## 8. 关键决策

1. 决策内容：沿用现有配置文件与同步脚本，不新增第二套配置入口。
2. 决策原因：降低系统复杂度，保持维护入口单一。
3. 影响模块：告警规则治理、配置管理流程。

## 9. 下迭代计划

1. 为参数配置补充 schema 校验与格式校验（时长、severity 枚举）。
2. 评估把 threshold governance 规则也并入同一参数化体系。
3. 输出配置变更审批清单（上线前检查项）。
