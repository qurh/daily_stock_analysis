# 迭代开发记录

迭代编号：`迭代29`  
日期：`2026-02-16`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 在趋势原因拆分能力基础上增加 TopN 限制。
2. 控制每个时间桶 `reason_counts` 返回规模，提升看板可读性与性能。
3. 保持趋势接口向后兼容。

## 2. 计划范围（Plan）

1. 为趋势接口增加 `reason_top_n` 参数。
2. 在服务层对 reason 统计结果进行桶内 TopN 截断。
3. 增加单测并完成回归验证。

## 3. 实际完成（Done）

1. 趋势接口增强：
   - `GET /api/v2/prompt-lock/failures/trends` 支持 `reason_top_n`
2. 生效规则：
   - 仅在 `split_by=reason` 时生效
3. 输出增强：
   - 返回 `reason_top_n` 字段
4. 服务层改造：
   - `PromptLockAuditService.failure_trends(..., reason_top_n=...)`
5. 新增并通过测试：
   - 每桶 reason 统计 TopN 截断正确性
6. 应用版本升级：
   - `0.3.14-m3-prompt-lock-trends-reason-topn`

## 4. 未完成项（Not Done）

1. 暂未支持按桶动态阈值过滤（如最小占比）。
2. 暂未支持 reason 聚类归并（同义错误码归并）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/services/prompt_lock_audit_service.py`
   - `refactor/backend/src/app/api/routes/prompt_lock.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-16-迭代29-M3-Prompt锁定趋势原因TopN.md`

## 6. 验证记录

1. 执行命令：
   - `pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py`
   - `pytest refactor/backend/tests/unit --maxfail=1`
   - `bash refactor/backend/scripts/ci.sh`
2. 结果摘要：
   - Prompt Lock 审计测试：通过
   - 后端单测：`64 passed`
   - CI：通过
3. 是否达到验收标准：
   - 达到（趋势接口支持 reason TopN）

## 7. 风险与问题

1. 风险描述：TopN 截断后会隐藏长尾原因，若无配套总览可能影响问题排查完整性。
2. 影响范围：治理看板异常分析深度。
3. 缓解措施：后续保留“查看全部原因”能力或增加 `reason_top_n` 可配置项。

## 8. 关键决策

1. 决策内容：`reason_top_n` 仅对 `split_by=reason` 生效，避免参数语义歧义。
2. 决策原因：保持接口行为清晰且兼容既有调用。
3. 影响模块：Prompt Lock 趋势统计接口契约与看板查询。

## 9. 下迭代计划

1. 评估并实现数据库侧分桶聚合，降低高并发下应用层聚合压力。
2. 设计治理看板组合查询接口（summary + grouped + trends 一次返回）。
3. 规划前端治理页联调与可视化验收。
