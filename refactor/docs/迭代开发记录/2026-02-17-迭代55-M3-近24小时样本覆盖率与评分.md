# 迭代开发记录

迭代编号：`迭代55`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 为回测样本质量补充近24小时覆盖率与评分观测。
2. 让近期样本质量告警可直接使用数值阈值。
3. 保持与现有样本门槛逻辑一致。

## 2. 计划范围（Plan）

1. 新增 24h 覆盖率指标。
2. 新增 24h 充分性评分指标。
3. 补充测试、版本与文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_coverage_ratio_pct_24h`
   - `refactor_backtest_records_return_sample_adequacy_score_24h`
2. 逻辑增强：
   - 抽取样本充分性计算函数，复用于 all-time 与 24h 口径。
   - 24h 评分沿用现有映射：`low=0.0`、`medium=0.5`、`high=1.0`。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_backtest_return_sample_24h_coverage_and_score`
4. 版本升级：
   - `0.3.40-m3-global-metrics-recent-sample-coverage-score`

## 4. 未完成项（Not Done）

1. 暂未输出 24h one-hot 等级指标。
2. 暂未扩展更多窗口（7d/30d）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代55-M3-近24小时样本覆盖率与评分.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k sample_24h_coverage_and_score`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k "sample_24h_coverage_and_score or sample_24h_threshold"`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（24h 覆盖率与评分指标可用）

## 7. 风险与问题

1. 风险描述：24h 样本很少时评分波动会较大。
2. 影响范围：近期告警灵敏度可能偏高。
3. 缓解措施：与 24h 样本量和门槛达标位联合判断。

## 8. 关键决策

1. 决策内容：24h 评分规则与全量评分规则保持一致。
2. 决策原因：降低口径差异导致的解释成本。
3. 影响模块：全局指标聚合、样本质量告警规则。

## 9. 下迭代计划

1. 评估接入 24h one-hot adequacy level 指标。
2. 评估窗口化样本质量趋势接口（24h/7d）。
3. 产出样本质量告警策略模板文档。
