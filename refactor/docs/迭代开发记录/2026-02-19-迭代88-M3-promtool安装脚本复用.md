# 迭代开发记录

迭代编号：`迭代88`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将 workflow 中重复的 promtool 安装逻辑抽取为复用脚本。
2. 保持固定版本与 checksum 校验能力不回退。
3. 降低 active/example workflow 维护成本。

## 2. 计划范围（Plan）

1. 先补失败测试，要求 workflow 调用统一安装脚本。
2. 新增 `install-promtool.sh` 并改造两个 workflow 调用。
3. 更新版本与文档。

## 3. 实际完成（Done）

1. 新增复用脚本：
   - `refactor/backend/scripts/install-promtool.sh`
   - 功能：下载固定版本、校验 SHA256、解压并安装 `promtool`。
2. Workflow 改造：
   - `.github/workflows/refactor-backend-ci.yml`
   - `refactor/backend/ci/github-actions/refactor-backend-ci.example.yml`
   - 从内联安装切换为调用复用脚本。
3. 测试增强：
   - 扩展 `test_ci_prometheus_rules_check.py`，新增脚本存在与关键行为断言。
4. 版本升级：
   - `0.3.73-m3-promtool-installer-script-reuse`

## 4. 未完成项（Not Done）

1. 暂未为安装脚本增加独立可执行 smoke 测试（当前为文本断言与 CI 间接验证）。
2. 暂未支持多架构自动识别（当前默认 `linux-amd64`）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/scripts/install-promtool.sh`
   - `.github/workflows/refactor-backend-ci.yml`
   - `refactor/backend/ci/github-actions/refactor-backend-ci.example.yml`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代88-M3-promtool安装脚本复用.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（先失败后通过）
   - CI 脚本：通过
3. 是否达到验收标准：
   - 达到（安装逻辑已复用且校验能力保留）

## 7. 风险与问题

1. 风险描述：脚本默认 `linux-amd64`，若 runner 架构变化需调整。
2. 影响范围：CI 运行环境兼容性。
3. 缓解措施：后续加入架构参数化或自动探测。

## 8. 关键决策

1. 决策内容：优先抽取可复用脚本，不在 workflow 中继续维护冗余命令块。
2. 决策原因：减少重复，降低错误率，便于后续统一升级版本与 checksum。
3. 影响模块：GitHub Actions workflow 与 CI 维护流程。

## 9. 下迭代计划

1. 为安装脚本补充多架构支持（如 `linux-arm64`）。
2. 增加脚本级 smoke 验证入口（可在 CI 独立步骤执行）。
3. 持续推进 M3 治理链路的稳定性与可观测性提升。
