# 迭代开发记录

迭代编号：`迭代97`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 在 CI workflow 层显式锁定远端校验门禁模式为 `strict`。
2. 通过测试约束 active/example workflow，避免后续无意识退化为非严格模式。
3. 保持现有远端校验能力和回归链路稳定。

## 2. 计划范围（Plan）

1. 先补失败测试，要求 workflow 必须包含 `PROMTOOL_VALIDATE_REMOTE_MODE: "strict"`。
2. 修改 active/example workflow 显式配置 `strict`。
3. 同步版本、README、CHANGELOG 并完成回归。

## 3. 实际完成（Done）

1. 测试增强：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - 在 workflow 断言中新增 `PROMTOOL_VALIDATE_REMOTE_MODE: "strict"` 校验。
2. Workflow 增强：
   - `.github/workflows/refactor-backend-ci.yml`
   - `refactor/backend/ci/github-actions/refactor-backend-ci.example.yml`
   - 在 `Run refactor backend ci.sh` 步骤显式设置 `PROMTOOL_VALIDATE_REMOTE_MODE: "strict"`。
3. 文档与版本：
   - `refactor/backend/src/app/main.py` 版本升级：`0.3.82-m3-promtool-workflow-explicit-strict-mode`
   - `refactor/backend/README.md` 补充 workflow 显式 strict 说明。
   - `refactor/docs/CHANGELOG.md` 增加本次变更记录。

## 4. 未完成项（Not Done）

1. 暂未在 workflow 增加按分支/环境自动切换 strict/soft 的策略（当前统一 strict）。
2. 暂未增加 soft 模式运行频率指标统计。

## 5. 代码与文档变更

1. 代码路径：
   - `.github/workflows/refactor-backend-ci.yml`
   - `refactor/backend/ci/github-actions/refactor-backend-ci.example.yml`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代97-M3-workflow显式严格门禁.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（21 passed）
   - CI 脚本：通过
3. 是否达到验收标准：
   - 达到（workflow 显式 strict 且受测试约束）

## 7. 风险与问题

1. 风险描述：统一 strict 在上游短时波动下可能放大 CI 失败率。
2. 影响范围：CI 稳定性与交付节奏。
3. 缓解措施：后续可在非生产流程引入受控 soft 策略并记录审计。

## 8. 关键决策

1. 决策内容：当前 workflow 统一显式 strict，不做环境分流。
2. 决策原因：优先保证质量门禁一致性与可追溯性。
3. 影响模块：CI workflow、promtool 配置治理测试。

## 9. 下迭代计划

1. 增加 soft 模式触发指标与日志聚合能力。
2. 评估非生产分支受控 soft 策略（明确开关与审计规则）。
3. 持续推进 M3 稳定性治理。
