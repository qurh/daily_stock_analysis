# 迭代开发记录

迭代编号：`迭代44`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 交付 M3 验收级闭环测试链路（提案 -> 回测 -> 人工确认 -> 生效 -> 回滚）。
2. 增加可重复执行的一键演练脚本。
3. 修复回滚后策略绑定状态治理缺口（避免悬挂 active 绑定）。

## 2. 计划范围（Plan）

1. 新增集成测试覆盖完整 M3 生命周期演练。
2. 新增演练脚本并遵循 `.env` 白名单加载策略。
3. 在策略回滚逻辑中同步处理绑定状态。
4. 同步 README、CHANGELOG、版本号。

## 3. 实际完成（Done）

1. 新增 M3 闭环集成测试：
   - `test_m3_strategy_backtest_publish_rollback_rehearsal_loop`
   - 路径：`refactor/backend/tests/integration/test_m3_acceptance_loop.py`
2. 新增一键演练脚本：
   - `refactor/backend/scripts/rehearse-m3-loop.sh`
   - 默认执行上述集成测试节点。
3. 回滚治理修复：
   - `StrategyService.rollback_strategy(...)` 回滚策略时自动将该策略 active 绑定改为 inactive。
4. 应用版本升级：
   - `0.3.29-m3-acceptance-rehearsal-loop`

## 4. 未完成项（Not Done）

1. 暂未将 integration 测试纳入默认 `scripts/ci.sh`（仍保持当前轻量 CI 范围）。
2. 暂未输出 API 级演练报告归档文件（当前由测试日志承担）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/services/strategy_service.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/integration/test_m3_acceptance_loop.py`
   - `refactor/backend/tests/unit/test_smoke_script_env.py`
3. 脚本路径：
   - `refactor/backend/scripts/rehearse-m3-loop.sh`
4. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代44-M3-验收闭环与回滚绑定治理.md`

## 6. 验证记录

1. 执行命令：
   - `pytest -q refactor/backend/tests/integration/test_m3_acceptance_loop.py::test_m3_strategy_backtest_publish_rollback_rehearsal_loop`
   - `cd refactor/backend && ./scripts/rehearse-m3-loop.sh`
   - `pytest -q refactor/backend/tests/unit/test_strategy_service.py -k publish_gate_and_rollback`
   - `pytest -q refactor/backend/tests/unit/test_smoke_script_env.py`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 集成演练测试：通过
   - 相关单测：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（M3 闭环可重复演练，回滚后绑定状态一致）

## 7. 风险与问题

1. 风险描述：M3 演练当前以集成测试方式运行，依赖测试环境可用性。
2. 影响范围：生产环境直连演练仍需额外运行编排与观测检查清单。
3. 缓解措施：后续补充 staging 演练 runbook 与固定化报告模板。

## 8. 关键决策

1. 决策内容：回滚时主动治理绑定状态，而不是仅依赖查询时过滤 strategy 状态。
2. 决策原因：避免管理接口出现逻辑“活跃但不可生效”的悬挂绑定。
3. 影响模块：策略生命周期管理、绑定查询结果一致性、运维治理体验。

## 9. 下迭代计划

1. 为回测/优化增加质量类观测指标（收益分布、参数覆盖率）。
2. 输出 Prompt 锁定指标与业务指标联动告警模板。
3. 评估将关键 integration 场景按开关纳入 CI 流水线。
