# 迭代开发记录

迭代编号：`迭代142`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 为 placeholder marker 配置文件新增独立校验脚本。
2. 将 marker 配置校验纳入 CI，提前阻断错误配置。

## 2. 计划范围（Plan）

1. 先补失败测试，约束脚本存在、默认通过、重复 marker 失败，以及 CI 接入。
2. 新增 marker 校验脚本并接入 `ci.sh`。
3. 同步文档、版本、迭代记录并完成全量验证。

## 3. 实际完成（Done）

1. TDD Red：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - 新增失败约束：
     - `ci.sh` 必须调用 `validate-validator-placeholder-markers.py`
     - marker 校验脚本默认配置通过
     - marker 校验脚本在重复 marker 情况下失败
2. TDD Green：
   - 新增脚本：
     - `refactor/backend/scripts/validate-validator-placeholder-markers.py`
   - 实现校验能力：
     - payload 为 object
     - `markers` 为非空列表
     - marker 必须为非空字符串
     - marker 格式校验（`^[A-Z][A-Z0-9_-]*$`）
     - marker 去空格并大小写归一后禁止重复
3. CI 接入：
   - `refactor/backend/scripts/ci.sh` 新增：
     - `python3 ./scripts/validate-validator-placeholder-markers.py`
4. 兼容性增强：
   - `sync-validator-error-codes.py` 读取 marker 配置时，重复 marker 改为直接失败。
5. 文档与版本：
   - `refactor/backend/README.md` 增加 marker 配置校验命令。
   - `refactor/docs/CHANGELOG.md` 新增 `0.3.127-m3-placeholder-marker-validator`。
   - `refactor/backend/src/app/main.py` 版本升级至 `0.3.127-m3-placeholder-marker-validator`。

## 4. 未完成项（Not Done）

1. marker 配置尚未使用 JSON Schema 文件进行声明式校验。
2. 尚未输出机器可解析的 json-errors 结构化错误。

## 5. 代码与文档变更

1. 脚本：
   - `refactor/backend/scripts/validate-validator-placeholder-markers.py`
   - `refactor/backend/scripts/sync-validator-error-codes.py`
   - `refactor/backend/scripts/ci.sh`
2. 测试：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代142-M3-marker配置校验与CI门禁.md`

## 6. 验证记录

1. Red 阶段：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "placeholder_markers_validator_script or invokes_prometheus_rules_check" -q`
   - 结果：预期失败（脚本不存在且 CI 未接入）。
2. Green 阶段：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "placeholder_markers_validator_script or invokes_prometheus_rules_check" -q`
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
   - 结果：全部通过。
3. 是否达到验收标准：
   - 达到（marker 配置校验已成为 CI 前置门禁）。

## 7. 风险与问题

1. 风险描述：当前 marker 格式规则较严格，未来若需更复杂 marker 命名需调整规则。
2. 影响范围：strict 描述检测可扩展性。
3. 缓解措施：后续将规则抽到独立 schema 并通过版本化管理。

## 8. 关键决策

1. 决策内容：先采用脚本校验快速落地 marker 配置门禁。
2. 决策原因：实现成本低、可直接接入既有 CI。
3. 影响模块：CI 质量门禁、placeholder marker 配置治理。

## 9. 下迭代计划

1. 为 marker 配置新增 schema 文件并补 schema 校验脚本。
2. 评估为 marker 校验脚本增加 `--json-errors` 输出模式。
3. 推进错误码目录结构化字段（severity/remediation）声明式校验。
