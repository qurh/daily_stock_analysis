# 迭代开发记录

迭代编号：`迭代11`  
日期：`2026-02-15`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 修复 `smoke-real-llm.sh` 在复杂 `.env` 内容下误判 `skip` 的问题。
2. 完成一次真实 DashScope smoke 调用验证。

## 2. 计划范围（Plan）

1. 对 smoke 脚本增加稳定的 `.env` 读取逻辑。
2. 增加回归测试约束脚本行为。
3. 执行真实 smoke 与全量回归。

## 3. 实际完成（Done）

1. 新增脚本级白名单 env 加载器：
   - 不再 `source` 执行整份 `.env`
   - 仅提取后端需要的配置键并 `export`
2. 新增回归测试断言：
   - `test_smoke_script_uses_key_whitelist_loader`
3. 修正 `.env` 模型配置：
   - `LLM_MODEL=qwen-plus`
4. 真实 smoke 已通过（DashScope 200 路径）。

## 4. 未完成项（Not Done）

1. 无。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/scripts/smoke-real-llm.sh`
   - `refactor/backend/tests/unit/test_smoke_script_env.py`
2. 文档路径：
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-15-迭代11-M2-smoke脚本健壮性修复.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && ./scripts/smoke-real-llm.sh`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_smoke_script_env.py`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest refactor/backend/tests/unit`
   - `cd refactor/backend && ./scripts/ci.sh`
2. 结果摘要：
   - 真实 smoke：通过
   - 脚本回归单测：通过
   - 后端单测：`21 passed`
   - CI：通过
3. 是否达到验收标准：
   - 达到

## 7. 风险与问题

1. 风险描述：白名单策略要求后续新增配置键时同步更新脚本。
2. 缓解措施：已增加脚本回归测试，避免配置加载回退到不安全方式。

## 8. 关键决策

1. 决策内容：脚本改为白名单解析 `.env`，禁止执行任意行。
2. 决策原因：提升安全性与兼容性，避免编码/注释导致加载异常。
3. 影响模块：运行脚本、集成 smoke 流程。

## 9. 下迭代计划

1. 将真实 smoke 结果纳入发布前检查清单。
2. 增加 DashScope 异常码分层告警。
