# 迭代开发记录

迭代编号：`迭代132`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将 summary example 一致性校验错误细化到字段级。
2. 提升 CI 失败时的定位效率。

## 2. 计划范围（Plan）

1. 先补失败测试定义字段级报错行为。
2. 在 schema validator 输出 expected/actual 明细。
3. 同步 README / CHANGELOG / 版本号。

## 3. 实际完成（Done）

1. TDD Red：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - 调整用例为字段级断言：
     - `changed_files_count mismatch`
     - `module total mismatch for strict`
2. TDD Green：
   - `refactor/backend/scripts/validate-strict-gate-summary-schema.py`
   - 一致性报错细化：
     - `changed_files_count mismatch (expected=..., actual=...)`
     - `total_added_lines mismatch (expected=..., actual=...)`
     - `total_removed_lines mismatch (expected=..., actual=...)`
     - `module total mismatch for <module> (expected=..., actual=...)`
3. 文档与版本：
   - `refactor/backend/README.md` 增加错误明细说明。
   - `refactor/docs/CHANGELOG.md` 新增 `0.3.117-m3-summary-consistency-error-details`。
   - `refactor/backend/src/app/main.py` 版本升级：`0.3.117-m3-summary-consistency-error-details`。

## 4. 未完成项（Not Done）

1. 暂未输出 JSON 格式错误报告。
2. 暂未做多错误聚合（当前遇到第一处错误即返回）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/scripts/validate-strict-gate-summary-schema.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代132-M3-summary一致性错误明细.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "inconsistent_totals or module_total_mismatch" -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 字段级报错相关用例通过
   - 后端全量 CI 通过
3. 是否达到验收标准：
   - 达到（一致性失败场景可快速定位到具体字段）

## 7. 风险与问题

1. 风险描述：错误文案后续变更可能影响依赖字符串断言的测试。
2. 影响范围：测试稳定性。
3. 缓解措施：保持关键短语稳定，必要时升级为结构化错误码。

## 8. 关键决策

1. 决策内容：优先输出 expected/actual 文本，而非立即引入结构化错误对象。
2. 决策原因：改动小、收益高、与现有 CLI 风格一致。
3. 影响模块：schema validator 可维护性与调试效率。

## 9. 下迭代计划

1. 增加 `--json-errors` 结构化错误输出。
2. 支持一次输出所有一致性问题，而非首错即停。
3. 将关键错误摘要写入 CI artifact。
