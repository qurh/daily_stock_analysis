# 迭代开发记录

迭代编号：`迭代145`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将 marker 校验器错误码纳入统一错误码目录治理链路。
2. 用测试强约束 catalog 分组、脚本注册表暴露与覆盖一致性。

## 2. 计划范围（Plan）

1. 先补失败测试，要求 `placeholder_markers` 分组与覆盖约束成立。
2. 改造 marker 校验器暴露 `VALIDATOR_ERROR_CODES` 并接入 sync 聚合。
3. 更新 catalog 描述、文档与版本并完成全量验证。

## 3. 实际完成（Done）

1. TDD Red：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - 新增失败约束：
     - catalog 必须包含 `placeholder_markers` 分组
     - marker 校验器必须暴露 `VALIDATOR_ERROR_CODES`
     - catalog 覆盖检查必须包含 marker 校验器脚本
2. TDD Green：
   - `refactor/backend/scripts/validate-validator-placeholder-markers.py`
   - 新增并统一使用 `VALIDATOR_ERROR_CODES` 注册表。
   - `refactor/backend/scripts/sync-validator-error-codes.py`
   - 新增 `placeholder_markers` 脚本来源分组。
   - `refactor/backend/config/validator-error-codes.json`
   - 新增 `placeholder_markers` 错误码分组并补全描述（无 TODO 占位）。
3. 文档与版本：
   - `refactor/backend/README.md` 增加 catalog 分组说明。
   - `refactor/docs/CHANGELOG.md` 新增 `0.3.130-m3-marker-error-code-catalog`。
   - `refactor/backend/src/app/main.py` 版本升级至 `0.3.130-m3-marker-error-code-catalog`。

## 4. 未完成项（Not Done）

1. marker 错误码分组尚未扩展为独立对外文档页面。
2. marker 校验器错误码尚未并入可机器读取的 error-code schema。

## 5. 代码与文档变更

1. 脚本：
   - `refactor/backend/scripts/validate-validator-placeholder-markers.py`
   - `refactor/backend/scripts/sync-validator-error-codes.py`
2. 配置：
   - `refactor/backend/config/validator-error-codes.json`
3. 测试：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
4. 文档：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代145-M3-marker错误码纳入统一目录治理.md`

## 6. 验证记录

1. Red 阶段：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "catalog_exists_and_has_prefix_groups or scripts_expose_error_code_registries or catalog_covers_all_script_error_codes" -q`
   - 结果：预期失败（catalog 缺失分组、脚本缺失注册表）。
2. Green 阶段：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "catalog_exists_and_has_prefix_groups or scripts_expose_error_code_registries or catalog_covers_all_script_error_codes" -q`
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "sync_script_strict_descriptions_pass_default_catalog or strict_descriptions_fail_on_todo or strict_descriptions_fail_on_tbd_fixme" -q`
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
   - 结果：全部通过。
3. 是否达到验收标准：
   - 达到（marker 错误码已进入统一目录并受测试与 CI 保护）。

## 7. 风险与问题

1. 风险描述：新增错误码后若未同步描述质量，strict 检查仍会阻断。
2. 影响范围：发布节奏与错误码文档一致性。
3. 缓解措施：保留 `--strict-descriptions` 门禁，后续补自动化描述质量 lint。

## 8. 关键决策

1. 决策内容：以脚本注册表为错误码集合来源，catalog 为描述来源。
2. 决策原因：可减少漏项并保持 CI 自动覆盖一致性。
3. 影响模块：错误码治理脚本、catalog、测试门禁。

## 9. 下迭代计划

1. 将 marker 错误码分组纳入对外 API/文档输出。
2. 为 catalog 引入结构化字段（severity/remediation）与 schema 校验。
3. 推进其他 validator 脚本统一接入该治理模型。
