# 迭代开发记录

迭代编号：`迭代131`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 增强 summary example payload 的逻辑一致性校验。
2. 防止示例文件通过 schema 但存在统计聚合错误。

## 2. 计划范围（Plan）

1. 先补失败测试定义一致性违规场景。
2. 在 schema validator 增加一致性规则。
3. 同步 README / CHANGELOG / 版本号。

## 3. 实际完成（Done）

1. TDD Red：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - 新增失败场景：example 中 `changed_files_count` 失真时校验必须失败。
2. TDD Green：
   - `refactor/backend/scripts/validate-strict-gate-summary-schema.py`
   - 新增一致性规则：
     - `changed_files_count == len(files)`
     - `total_added_lines == sum(files[].added_lines)`
     - `total_removed_lines == sum(files[].removed_lines)`
     - 模块总数 == 每文件模块计数聚合
3. 文档与版本：
   - `refactor/backend/README.md` 增加一致性校验说明。
   - `refactor/docs/CHANGELOG.md` 新增 `0.3.116-m3-summary-example-consistency-guard`。
   - `refactor/backend/src/app/main.py` 版本升级：`0.3.116-m3-summary-example-consistency-guard`。

## 4. 未完成项（Not Done）

1. 暂未校验 example 的字段排序与格式规范。
2. 暂未输出一致性校验的差异明细（目前返回通用错误）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/scripts/validate-strict-gate-summary-schema.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代131-M3-summary示例一致性校验.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "inconsistent_totals" -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 一致性违规场景测试通过（能够正确失败）
   - 后端全量 CI 通过
3. 是否达到验收标准：
   - 达到（example payload 具备结构+逻辑双重校验）

## 7. 风险与问题

1. 风险描述：错误信息当前较概括，不利于快速定位字段。
2. 影响范围：开发调试效率。
3. 缓解措施：后续补充差异定位信息（期望值/实际值）。

## 8. 关键决策

1. 决策内容：先引入一致性硬校验，再逐步细化错误信息。
2. 决策原因：优先堵住错误样本进入主干。
3. 影响模块：契约稳定性和 CI 质量门禁。

## 9. 下迭代计划

1. 细化一致性错误输出（字段级 diff）。
2. 增加 example 自动刷新脚本，减少人工维护偏差。
3. 将一致性校验摘要输出为 CI artifact。
