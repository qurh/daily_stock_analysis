# 迭代开发记录

迭代编号：`迭代23`  
日期：`2026-02-16`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 提供 strict 失败的审计查询能力。
2. 提供失败原因聚合统计能力，支持后续治理。
3. 让 chat 与 analysis 两条链路统一落审计事件。

## 2. 计划范围（Plan）

1. 新增 prompt lock 审计持久化表。
2. 新增审计服务与查询 API。
3. 在 chat/analysis strict 失败路径接入审计写入。

## 3. 实际完成（Done）

1. 新增持久化表：
   - `prompt_lock_events`
2. 新增服务：
   - `PromptLockAuditService`
   - 支持 `record_event / list_events / summarize_failures`
3. 新增 API：
   - `GET /api/v2/prompt-lock/events`
   - `GET /api/v2/prompt-lock/failures/summary`
4. 失败落审计：
   - chat strict 失败记录 `source_type=chat`
   - analysis strict 失败记录 `source_type=analysis`
5. 新增并通过测试：
   - chat strict 失败可查询
   - summary 聚合失败原因可用
6. 应用版本升级：
   - `0.3.8-m3-prompt-lock-audit`

## 4. 未完成项（Not Done）

1. 暂未引入时间窗口维度（如 `last_24h`）的聚合查询参数。
2. 暂未将审计结果接入前端策略治理页面。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/services/prompt_lock_audit_service.py`
   - `refactor/backend/src/app/services/chat_service.py`
   - `refactor/backend/src/app/services/analysis_service.py`
   - `refactor/backend/src/app/api/routes/prompt_lock.py`
   - `refactor/backend/src/app/api/router.py`
   - `refactor/backend/src/app/api/deps.py`
   - `refactor/backend/src/app/persistence/sqlite_db.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-16-迭代23-M3-Prompt锁定审计与聚合.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_strategy_context_injection.py refactor/backend/tests/unit/test_chat_service.py refactor/backend/tests/unit/test_analysis_jobs.py`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest refactor/backend/tests/unit --maxfail=1`
   - `cd refactor/backend && ./scripts/ci.sh`
2. 结果摘要：
   - 审计定向测试：通过
   - 后端单测：`49 passed`
   - CI：通过
3. 是否达到验收标准：
   - 达到（strict 失败审计与聚合能力可用）

## 7. 风险与问题

1. 风险描述：审计聚合目前以内存统计实现，超大规模事件下存在性能压力。
2. 影响范围：未来高并发或长时间运行场景。
3. 缓解措施：后续补 SQL 聚合与窗口分页策略。

## 8. 关键决策

1. 决策内容：优先落地可用审计查询接口，再做性能优化。
2. 决策原因：当前阶段先满足治理闭环和可观测诉求。
3. 影响模块：Prompt Lock、Chat、Analysis、治理接口层。

## 9. 下迭代计划

1. 增加按时间窗口和 flow/source 维度的聚合参数。
2. 将审计接口接入前端策略治理页面。
3. 推进 M3 端到端演练自动化（提案->回测->审批->生效->回滚）。
