# 迭代开发记录

迭代编号：`迭代58`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 在 7d 样本门槛指标基础上补齐覆盖率和评分。
2. 支持 7d 样本质量告警直接使用数值阈值。
3. 与全量/24h 窗口保持统一口径。

## 2. 计划范围（Plan）

1. 新增 7d 覆盖率指标。
2. 新增 7d 充分性评分指标。
3. 补充测试并同步文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_coverage_ratio_pct_7d`
   - `refactor_backtest_records_return_sample_adequacy_score_7d`
2. 统计逻辑增强：
   - 基于 7d 样本计数复用统一充分性计算函数得到 coverage/score。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_backtest_return_sample_7d_coverage_and_score`
4. 版本升级：
   - `0.3.43-m3-global-metrics-7d-sample-coverage-score`

## 4. 未完成项（Not Done）

1. 暂未输出 7d one-hot adequacy level。
2. 暂未支持可配置窗口（当前固定 24h / 7d）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代58-M3-近7天样本覆盖率与评分.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k sample_7d_coverage_and_score`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k "sample_7d_coverage_and_score or sample_7d_threshold"`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（7d 覆盖率与评分指标可用）

## 7. 风险与问题

1. 风险描述：7d 窗口样本在节假日/停牌期可能骤降。
2. 影响范围：评分短期波动增大。
3. 缓解措施：与 7d sample_size、threshold_met 联合判读。

## 8. 关键决策

1. 决策内容：7d coverage/score 与现有全量/24h规则完全复用。
2. 决策原因：避免多口径带来的解释复杂度。
3. 影响模块：metrics 聚合与告警策略配置。

## 9. 下迭代计划

1. 评估补齐 7d one-hot adequacy level 指标。
2. 评估输出多窗口联动质量模板（全量/24h/7d）。
3. 评估是否需要开放窗口参数配置。
