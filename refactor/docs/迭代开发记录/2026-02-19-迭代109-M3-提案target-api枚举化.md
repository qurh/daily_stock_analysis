# 迭代开发记录

迭代编号：`迭代109`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将提案 `target` 约束前移到 API 请求模型层。
2. 对齐 API 层与服务层 target 白名单，避免双层规则漂移。
3. 保持 M3 提案审批与发布闭环稳定。

## 2. 计划范围（Plan）

1. 先补 OpenAPI 合约失败测试，定义 target 枚举。
2. 在 `OptimizationProposalCreateRequest` 中将 `target` 改为枚举类型。
3. 服务层 target 分类改为白名单映射，形成 defense-in-depth。

## 3. 实际完成（Done）

1. TDD Red：
   - `refactor/backend/tests/unit/test_feedback_optimization_service.py`
   - 新增 `test_optimization_proposal_target_openapi_enum_contract`，初始失败。
2. TDD Green：
   - `refactor/backend/src/app/api/routes/optimization.py`
     - 新增 `OptimizationProposalTarget` 枚举。
     - `OptimizationProposalCreateRequest.target` 改为枚举类型。
   - `refactor/backend/src/app/services/optimization_service.py`
     - target 分类逻辑改为显式白名单映射，和 API 枚举一致。
3. 测试更新：
   - 不支持 target 用例更新为请求层校验语义（`422`）。
4. 文档与版本：
   - `refactor/backend/README.md` 更新 target 枚举与校验层次说明。
   - `refactor/docs/CHANGELOG.md` 新增 `0.3.94-m3-proposal-target-enum-api`。
   - `refactor/backend/src/app/main.py` 版本升级：`0.3.94-m3-proposal-target-enum-api`。

## 4. 未完成项（Not Done）

1. 暂未将 `target` 枚举抽离为跨前后端共享常量包。
2. 暂未给旧客户端提供 target 自动迁移映射层。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/optimization.py`
   - `refactor/backend/src/app/services/optimization_service.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_feedback_optimization_service.py`
   - `refactor/backend/tests/unit/test_strategy_service.py`（回归通过）
   - `refactor/backend/tests/integration/test_m3_acceptance_loop.py`（回归通过）
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代109-M3-提案target-api枚举化.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_feedback_optimization_service.py -q`
   - `cd refactor/backend && pytest tests/unit/test_strategy_service.py -q`
   - `cd refactor/backend && pytest tests/integration/test_m3_acceptance_loop.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 提案相关单测：通过
   - 策略与 M3 集成回归：通过
   - CI 脚本：通过
3. 是否达到验收标准：
   - 达到（target 枚举化已生效，服务层白名单与 API 对齐）

## 7. 风险与问题

1. 风险描述：旧客户端发送非枚举 target 将被请求层直接拦截。
2. 影响范围：`/api/v2/optimization/proposals` 请求兼容性。
3. 缓解措施：文档已更新，后续可增加迁移映射或客户端提示。

## 8. 关键决策

1. 决策内容：将 target 约束前移到 API 模型，并保留服务层兜底。
2. 决策原因：输入治理更早失败、更清晰，同时保持防御式编程。
3. 影响模块：优化提案入口、OpenAPI 合约、下游提案治理。

## 9. 下迭代计划

1. 统一错误码枚举并补 `FDB-INPUT-003/004/005` 的可观测指标。
2. 评估将 `target` 与 `diff` schema 抽象为可扩展注册表。
3. 为前端/机器人侧输出枚举常量，减少硬编码漂移。
