# 迭代开发记录

迭代编号：`迭代200`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 收敛 sync 脚本 `--json-errors` 场景下未知 CLI 参数的非结构化失败输出。
2. 确保参数错误时 stderr 仍可被 JSON 解析消费。
3. 保持错误码体系不扩散，沿用既有 `unexpected_error`。

## 2. 计划范围（Plan）

1. 新增未知参数场景失败测试（RED）。
2. 最小改动参数解析逻辑，输出结构化 JSON 错误（GREEN）。
3. 回归并同步 README/CHANGELOG/版本号。

## 3. 实际完成（Done）

1. TDD Red：
   - 新增测试：
     - `test_validator_error_code_sync_script_json_errors_for_unknown_cli_arguments`
   - Red 结果：
     - 现状 stderr 为 argparse usage 文本，`json.loads` 失败（预期暴露问题）。
2. TDD Green：
   - `refactor/backend/scripts/sync-validator-error-codes.py`
     - `main()` 改为 `parse_known_args`；
     - 对未知参数分支，在 `--json-errors` 下输出结构化 JSON 并返回 `1`；
     - 错误码使用 `error_code_sync_validator_error_codes_unexpected_error`，
       context 补充：
       - `stage=argument_parsing`
       - `exception_type=SystemExit`
       - `exit_code=2`
       - `unknown_args=[...]`
3. 额外覆盖：
   - 新增 `--check + --json-errors + 目录路径` 用例，明确校验 catalog read failed JSON 输出契约。
4. 文档与版本：
   - `refactor/backend/README.md` 补充未知参数 JSON 错误说明。
   - `refactor/docs/CHANGELOG.md` 新增 `0.3.185-m3-sync-json-errors-cli-unknown-arguments`。
   - `refactor/backend/src/app/main.py` 版本升级至 `0.3.185-m3-sync-json-errors-cli-unknown-arguments`。

## 4. 未完成项（Not Done）

1. 仍保留最终 `unexpected_error` 兜底分支，后续可继续细分到更具体错误码。

## 5. 代码与文档变更

1. 修改：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - `refactor/backend/scripts/sync-validator-error-codes.py`
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/backend/src/app/main.py`
2. 新增：
   - `refactor/docs/迭代开发记录/2026-02-19-迭代200-M3-sync-未知参数json错误结构化.md`

## 6. 验证记录

1. RED：
   - `pytest -q refactor/backend/tests/unit/test_ci_prometheus_rules_check.py -k "unknown_cli_arguments"`
   - 结果：失败（预期，stderr 非 JSON）。
2. GREEN（目标回归）：
   - 同命令回归通过。
3. 回归：
   - `pytest -q refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - `cd refactor/backend && python3 -m compileall -q src scripts`
   - 结果：通过。

## 7. 风险与问题

1. 风险描述：未知参数在 `--json-errors` 下不再输出 argparse usage 详情，改为结构化错误文本。
2. 影响范围：依赖 argparse usage 文本的调用脚本。
3. 缓解措施：`context.unknown_args` 保留完整参数列表，且 plain 模式行为不变。

## 8. 关键决策

1. 决策内容：未知参数仅在 `--json-errors` 场景结构化处理；普通模式继续走 argparse 原生错误。
2. 决策原因：兼顾机器消费一致性与命令行可读性。
3. 影响模块：sync 脚本参数解析入口。

## 9. 下迭代计划

1. 继续收敛 sync 脚本中剩余 `unexpected_error` 兜底触发面。
2. 统一 `--json-errors` 各场景 context 字段最小基线。
