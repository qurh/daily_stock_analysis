# 迭代开发记录

迭代编号：`迭代57`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 在全量与 24h 样本观测基础上，补齐近 7 天窗口核心指标。
2. 支持判断“短期一周内样本是否满足门槛”。
3. 保持样本门槛规则口径一致。

## 2. 计划范围（Plan）

1. 新增 `7d` 样本量指标。
2. 新增 `7d` 门槛达标指标。
3. 增加单测与文档同步。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_size_7d`
   - `refactor_backtest_records_return_sample_size_threshold_met_7d`
2. 统计逻辑增强：
   - 基于 `created_at` 新增 7 天窗口样本计数。
   - 复用 `BACKTEST_RETURN_SAMPLE_MIN_SIZE` 判定 7d 门槛达标。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_backtest_return_sample_7d_threshold`
4. 版本升级：
   - `0.3.42-m3-global-metrics-7d-sample-threshold`

## 4. 未完成项（Not Done）

1. 暂未输出 7d 覆盖率和 7d 评分指标。
2. 暂未输出 7d one-hot 等级指标。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代57-M3-近7天样本门槛指标.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k sample_7d_threshold`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k "sample_7d_threshold or sample_24h_adequacy_level"`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（7d 样本量与门槛达标指标可用）

## 7. 风险与问题

1. 风险描述：7d 样本数据可能受交易日分布影响出现阶段性波动。
2. 影响范围：周内节假日或停牌期告警信号稳定性下降。
3. 缓解措施：与 24h 与全量指标联合判断。

## 8. 关键决策

1. 决策内容：本迭代先交付 7d 核心门槛指标，不引入更多派生统计。
2. 决策原因：优先满足窗口化门槛判断主诉求，控制复杂度。
3. 影响模块：全局 metrics 聚合、监控规则编排。

## 9. 下迭代计划

1. 评估补齐 7d 覆盖率与 7d 充分性评分。
2. 评估 7d one-hot 等级指标是否需要接入。
3. 输出多窗口（全量/24h/7d）告警组合模板。
