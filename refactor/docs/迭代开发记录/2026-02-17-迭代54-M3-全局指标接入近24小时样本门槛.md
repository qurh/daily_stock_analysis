# 迭代开发记录

迭代编号：`迭代54`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 在全局样本门槛指标基础上补充“近24小时”视角。
2. 识别“历史样本足够但近期样本不足”的风险场景。
3. 保持指标结构兼容并复用现有门槛配置。

## 2. 计划范围（Plan）

1. 新增近24小时样本量指标。
2. 新增近24小时样本门槛达标指标。
3. 补充测试与文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_size_24h`
   - `refactor_backtest_records_return_sample_size_threshold_met_24h`
2. 统计逻辑增强：
   - 基于 `backtest_records.created_at` 计算近24小时样本计数。
   - 复用 `BACKTEST_RETURN_SAMPLE_MIN_SIZE` 作为 24h 门槛判断依据。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_backtest_return_sample_24h_threshold`
4. 版本升级：
   - `0.3.39-m3-global-metrics-recent-sample-threshold`

## 4. 未完成项（Not Done）

1. 暂未引入 24h 覆盖率/充分性评分（当前仅样本量与达标位）。
2. 暂未支持 7d/30d 等更多时间窗口。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代54-M3-全局指标接入近24小时样本门槛.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k sample_24h_threshold`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k "sample_24h_threshold or adequacy_score"`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（全局指标新增近24小时样本门槛观测）

## 7. 风险与问题

1. 风险描述：`created_at` 非法时间格式会被忽略，可能低估 24h 样本量。
2. 影响范围：24h 门槛判断可能偏保守。
3. 缓解措施：后续可补充时间格式校验与数据清洗告警。

## 8. 关键决策

1. 决策内容：24h 口径先仅输出样本量与门槛达标位。
2. 决策原因：优先补齐关键判断信号，控制指标复杂度。
3. 影响模块：全局指标聚合、近期样本告警规则。

## 9. 下迭代计划

1. 评估接入 24h 覆盖率与 adequacy score 指标。
2. 评估扩展 7d/30d 窗口指标的成本与收益。
3. 完成样本指标联动告警模板文档。
