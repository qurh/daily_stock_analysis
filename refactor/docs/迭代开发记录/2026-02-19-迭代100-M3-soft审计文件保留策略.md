# 迭代开发记录

迭代编号：`迭代100`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 为 promtool soft 审计文件增加保留策略，避免文件无限增长。
2. 提供可配置的最大保留行数，默认保持兼容（不裁剪）。
3. 用测试锁定裁剪行为与参数校验行为。

## 2. 计划范围（Plan）

1. 先补失败测试，定义“按最大行数保留”与“非法参数报错”行为。
2. 增强 `validate-promtool-installer-config.sh` 支持并执行审计文件裁剪。
3. 同步版本、README、CHANGELOG 与 `.env.example`。

## 3. 实际完成（Done）

1. 脚本增强：
   - `refactor/backend/scripts/validate-promtool-installer-config.sh`
   - 新增环境变量：`PROMTOOL_REMOTE_SOFT_AUDIT_MAX_LINES`（默认 `0`）。
   - 当 `PROMTOOL_REMOTE_SOFT_AUDIT_FILE` 已配置且 `MAX_LINES > 0` 时，写入后自动裁剪到最新 N 行。
   - 增加 `PROMTOOL_REMOTE_SOFT_AUDIT_MAX_LINES` 非负整数校验。
2. 测试增强：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - 新增用例：
     - 审计文件裁剪到最大保留行数。
     - 非法 `PROMTOOL_REMOTE_SOFT_AUDIT_MAX_LINES` 值导致校验失败。
3. 文档与版本：
   - `refactor/backend/src/app/main.py` 版本升级：`0.3.85-m3-promtool-soft-audit-retention`
   - `refactor/backend/.env.example` 增加 `PROMTOOL_REMOTE_SOFT_AUDIT_MAX_LINES` 说明。
   - `refactor/backend/README.md` 增加审计保留配置说明。
   - `refactor/docs/CHANGELOG.md` 增加本次变更记录。

## 4. 未完成项（Not Done）

1. 暂未实现基于文件大小或时间窗口的复合裁剪策略。
2. 暂未将审计事件迁移到结构化存储（当前仍为文件文本）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/scripts/validate-promtool-installer-config.sh`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/.env.example`
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代100-M3-soft审计文件保留策略.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（24 passed）
   - CI 脚本：通过
3. 是否达到验收标准：
   - 达到（soft 审计文件支持可配置保留行数与参数校验）

## 7. 风险与问题

1. 风险描述：当前按行裁剪，若单条审计记录异常长，仍可能占用较大空间。
2. 影响范围：审计文件磁盘占用。
3. 缓解措施：后续可增加按大小裁剪与压缩归档策略。

## 8. 关键决策

1. 决策内容：先采用“按行数保留”作为最小可行保留策略。
2. 决策原因：实现简单、可预测、对现有链路侵入最小。
3. 影响模块：promtool soft 审计写入与文件治理。

## 9. 下迭代计划

1. 评估按大小/时间窗口的复合轮转策略。
2. 评估审计记录迁移到数据库并做高效聚合。
3. 继续推进 M3 可观测性治理闭环。
