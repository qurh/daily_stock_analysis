# 迭代开发记录

迭代编号：`迭代59`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 补齐 7d 样本充分性 one-hot 等级观测能力。
2. 形成全量/24h/7d 三窗口一致的等级指标体系。
3. 保持指标解释规则统一。

## 2. 计划范围（Plan）

1. 新增 7d one-hot adequacy level 指标。
2. 新增对应单测并验证。
3. 更新版本与文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_adequacy_level_7d{level=low|medium|high}`
2. 逻辑增强：
   - 回测质量快照新增 `return_sample_adequacy_levels_onehot_7d`。
   - 7d 等级规则复用统一充分性计算函数（与全量/24h一致）。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_backtest_return_sample_7d_adequacy_level`
4. 版本升级：
   - `0.3.44-m3-global-metrics-7d-adequacy-level`

## 4. 未完成项（Not Done）

1. 暂未提供可配置窗口参数（当前固定全量/24h/7d）。
2. 暂未提供窗口级趋势接口。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代59-M3-近7天样本充分性等级.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k sample_7d_adequacy_level`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_prompt_lock_audit.py -k "sample_7d_adequacy_level or sample_7d_coverage_and_score"`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（7d one-hot adequacy level 指标可用）

## 7. 风险与问题

1. 风险描述：多窗口指标增多后告警规则配置复杂度上升。
2. 影响范围：规则维护成本增加。
3. 缓解措施：后续整理多窗口告警模板并统一命名规范。

## 8. 关键决策

1. 决策内容：坚持三窗口规则同构（all-time/24h/7d）。
2. 决策原因：降低解释成本与运维混淆风险。
3. 影响模块：metrics 聚合与告警编排层。

## 9. 下迭代计划

1. 输出“多窗口样本质量联动告警模板”文档。
2. 评估窗口参数化方案可行性。
3. 评估是否需要引入 30d 窗口。
