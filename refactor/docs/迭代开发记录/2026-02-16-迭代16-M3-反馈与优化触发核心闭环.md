# 迭代开发记录

迭代编号：`迭代16`  
日期：`2026-02-16`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 进入 M3-2，完成反馈采集与优化触发核心闭环。
2. 打通 chatbot 提案进入优化审核链路。
3. 在保持可扩展边界前提下优先交付业务最小可用。

## 2. 计划范围（Plan）

1. 新增反馈、优化任务、提案审核数据结构与服务。
2. 开放反馈与优化核心 API。
3. 按 TDD 完成核心业务场景测试并回归。

## 3. 实际完成（Done）

1. 新增持久化表：
   - `feedback_records`
   - `optimization_jobs`
   - `change_proposals`
2. 新增 `FeedbackService`：
   - 反馈记录入库
   - 反馈查询
   - 反馈特征快照聚合
3. 新增 `OptimizationService`：
   - 优化任务触发（事件/手动/chatbot）
   - 消费反馈 + 回测特征生成质量评分
   - chatbot/手动提案创建
   - 提案 `approve/reject` 审核流转与状态冲突保护
4. 新增 API：
   - `POST /api/v2/feedback/records`
   - `GET /api/v2/feedback/records`
   - `POST /api/v2/optimization/jobs/trigger`
   - `POST /api/v2/optimization/proposals`
   - `POST /api/v2/optimization/proposals/{proposal_id}/approve`
   - `POST /api/v2/optimization/proposals/{proposal_id}/reject`
5. 完成 `main/deps/router` 接线，纳入应用状态与依赖注入。
6. 新增单测：
   - feedback 创建与查询
   - optimization trigger 读取 feedback + backtest 特征
   - chatbot 提案审核流与冲突校验

## 4. 未完成项（Not Done）

1. 暂未实现优化任务历史查询 API（当前满足触发与执行闭环）。
2. 暂未实现提案灰度发布/回滚执行链路（后续 M3/M4 扩展）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/services/feedback_service.py`
   - `refactor/backend/src/app/services/optimization_service.py`
   - `refactor/backend/src/app/api/routes/feedback.py`
   - `refactor/backend/src/app/api/routes/optimization.py`
   - `refactor/backend/src/app/persistence/sqlite_db.py`
   - `refactor/backend/src/app/api/router.py`
   - `refactor/backend/src/app/api/deps.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_feedback_optimization_service.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-16-迭代16-M3-反馈与优化触发核心闭环.md`

## 6. 验证记录

1. 执行命令：
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit/test_feedback_optimization_service.py`
   - `PYTHONPATH=refactor/backend/src python3 -m pytest -q refactor/backend/tests/unit`
   - `cd refactor/backend && ./scripts/ci.sh`
2. 结果摘要：
   - M3-2 定向测试：`3 passed`
   - 后端单测：`34 passed`
   - CI：通过
3. 是否达到验收标准：
   - 达到（M3-2 核心闭环可用）

## 7. 风险与问题

1. 风险描述：当前优化评估采用最小评分逻辑，策略有效性评估仍偏轻量。
2. 缓解措施：后续将回测明细、反馈标签与策略实验结果并入统一评估器。

## 8. 关键决策

1. 决策内容：先实现“触发 -> 评估 -> 提案审核”最小闭环，再补发布治理链路。
2. 决策原因：遵循当前“业务优先、工程化后置”策略。
3. 影响模块：Feedback、Optimization、Backtest 联动。

## 9. 下迭代计划

1. 进入 M3-3：认知沉淀与策略提炼（chat 认知入库 + 策略产物化）。
2. 补充优化任务/提案查询接口，支持前端控制台最小展示。
3. 对接策略生效闸门（通过评估才能激活）。
