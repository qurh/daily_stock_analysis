# 迭代开发记录

迭代编号：`迭代86`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将 CI 中的 `promtool` 安装从 `apt` 方式改为固定版本二进制方式。
2. 提升 `refactor/backend` 质量门禁链路的可复现性。
3. 通过测试锁定安装方式，防止后续回退。

## 2. 计划范围（Plan）

1. 先新增失败测试，要求 workflow 使用固定版本下载方式。
2. 修改 active/example workflow 的安装步骤。
3. 同步版本和文档。

## 3. 实际完成（Done）

1. 测试增强：
   - 扩展 `test_github_actions_refactor_ci_example_includes_promtool_install_and_ci_run`
   - 扩展 `test_github_actions_refactor_ci_workflow_exists_and_targets_backend_paths`
   - 新增断言：固定版本 `PROMTOOL_VERSION=2.52.0` + GitHub release 下载 + 非 apt 安装。
2. Workflow 升级：
   - `.github/workflows/refactor-backend-ci.yml`
   - `refactor/backend/ci/github-actions/refactor-backend-ci.example.yml`
   - 均改为下载 `prometheus-<version>.linux-amd64.tar.gz` 并安装 `promtool`。
3. 版本升级：
   - `0.3.71-m3-promtool-fixed-version-workflow`

## 4. 未完成项（Not Done）

1. 暂未增加二进制校验和（checksum）校验。
2. 暂未抽取复用脚本统一管理 workflow 内安装逻辑。

## 5. 代码与文档变更

1. 代码路径：
   - `.github/workflows/refactor-backend-ci.yml`
   - `refactor/backend/ci/github-actions/refactor-backend-ci.example.yml`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代86-M3-promtool固定版本安装.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（先失败后通过）
   - CI 脚本：通过
3. 是否达到验收标准：
   - 达到（workflow 已采用固定版本安装，且有自动化测试约束）

## 7. 风险与问题

1. 风险描述：固定版本需后续人工升级，可能滞后于安全更新。
2. 影响范围：CI 工具链版本更新节奏。
3. 缓解措施：后续增加版本治理策略（定期升级 + 回归验证）。

## 8. 关键决策

1. 决策内容：采用固定版本二进制下载替代 apt 安装。
2. 决策原因：降低包仓库波动导致的构建不稳定，提升可复现性。
3. 影响模块：GitHub Actions workflow 与 CI 稳定性。

## 9. 下迭代计划

1. 为 promtool 下载链路增加 checksum 校验。
2. 将安装逻辑下沉为复用脚本，减少 workflow 重复配置。
3. 继续推进 M3 治理链路的告警联动与可观测增强。
