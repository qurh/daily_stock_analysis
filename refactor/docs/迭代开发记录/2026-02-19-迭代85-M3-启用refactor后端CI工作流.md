# 迭代开发记录

迭代编号：`迭代85`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将 `refactor/backend` 的 CI 示例模板升级为根目录可执行 workflow。
2. 确保 workflow 仅在 `refactor/backend` 与相关文档改动时触发。
3. 通过自动化测试锁定 workflow 文件关键内容。

## 2. 计划范围（Plan）

1. 先补失败测试，约束根 workflow 文件存在且包含关键步骤。
2. 新增根目录 workflow：`.github/workflows/refactor-backend-ci.yml`。
3. 同步版本和文档。

## 3. 实际完成（Done）

1. 新增根目录 workflow：
   - `.github/workflows/refactor-backend-ci.yml`
   - 包含 `promtool` 安装与 `refactor/backend/scripts/ci.sh` 执行。
2. 测试增强：
   - 新增 `test_github_actions_refactor_ci_workflow_exists_and_targets_backend_paths`。
3. 文档更新：
   - README 同步 active workflow 与 template 路径。
4. 版本升级：
   - `0.3.70-m3-refactor-backend-github-actions-workflow`

## 4. 未完成项（Not Done）

1. 暂未加上 workflow 级缓存优化（如 pip/apt 高级缓存策略）。
2. 暂未覆盖 push 分支策略（当前以 PR + 手动触发为主）。

## 5. 代码与文档变更

1. 代码路径：
   - `.github/workflows/refactor-backend-ci.yml`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代85-M3-启用refactor后端CI工作流.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（先失败后通过）
   - CI 脚本：通过
3. 是否达到验收标准：
   - 达到（根 workflow 已可用，关键行为有测试约束）

## 7. 风险与问题

1. 风险描述：`apt-get install prometheus` 依赖外部包源稳定性。
2. 影响范围：GitHub Actions 启动与依赖安装阶段。
3. 缓解措施：后续可切换到固定版本下载脚本并校验校验和。

## 8. 关键决策

1. 决策内容：直接启用 root workflow，而不是仅保留模板。
2. 决策原因：前一迭代已验证模板质量，当前阶段优先确保实际门禁生效。
3. 影响模块：CI 质量门禁与 refactor 后端交付链路。

## 9. 下迭代计划

1. 评估 workflow 的 pip 缓存和 apt 安装稳定性优化。
2. 评估将 promtool 安装改为固定版本二进制方式。
3. 继续推进 M3 治理可观测性的告警联动完善。
