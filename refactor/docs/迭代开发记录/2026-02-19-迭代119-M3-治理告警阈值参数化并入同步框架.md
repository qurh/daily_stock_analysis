# 迭代开发记录

迭代编号：`迭代119`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将 threshold-governance 告警参数并入现有阈值配置与同步框架。
2. 保持告警表达式不变，仅将 `for` 与 `severity` 从配置统一驱动。
3. 让 strict-gate / governance / soft-audit 三类告警统一通过一套配置同步。

## 2. 计划范围（Plan）

1. 先补失败测试，要求配置中包含 governance 参数字段。
2. 更新阈值配置文件，补齐四个 profile 的 governance 参数。
3. 扩展同步脚本，新增 governance 三条告警参数同步。
4. 同步 README、CHANGELOG、版本信息。

## 3. 实际完成（Done）

1. TDD Red：
   - `refactor/backend/tests/unit/test_strict_gate_alert_threshold_sync.py`
   - 新增 governance 字段断言，初始失败（缺字段）。
2. TDD Green：
   - `refactor/backend/config/strict-gate-alert-thresholds.json`
     - 新增 6 个 governance 参数字段：
       - `governance_warn_for`
       - `governance_warn_severity`
       - `governance_critical_for`
       - `governance_critical_severity`
       - `governance_normalization_for`
       - `governance_normalization_severity`
   - `refactor/backend/scripts/sync-strict-gate-alert-thresholds.py`
     - 新增 governance 三条告警的 `for` 与 `severity` 同步逻辑。
3. 文档与版本：
   - `refactor/backend/README.md` 更新配置覆盖范围说明。
   - `refactor/docs/CHANGELOG.md` 新增 `0.3.104-m3-governance-threshold-param-sync`。
   - `refactor/backend/src/app/main.py` 版本升级：`0.3.104-m3-governance-threshold-param-sync`。

## 4. 未完成项（Not Done）

1. 暂未将 governance ratio 阈值（业务配置）与告警模板配置统一到同一来源。
2. 暂未输出参数化同步变更差异摘要（dry-run diff 视图）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/config/strict-gate-alert-thresholds.json`
   - `refactor/backend/scripts/sync-strict-gate-alert-thresholds.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_strict_gate_alert_threshold_sync.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代119-M3-治理告警阈值参数化并入同步框架.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_strict_gate_alert_threshold_sync.py -q`
   - `cd refactor/backend && python3 scripts/sync-strict-gate-alert-thresholds.py --check`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 新增 governance 参数断言通过
   - 同步检查通过
   - 全量 CI 通过（promtool 缺失按现有策略 skip）
3. 是否达到验收标准：
   - 达到（governance 告警参数已纳入统一参数化同步）

## 7. 风险与问题

1. 风险描述：同一配置文件承载多类告警参数，变更影响面扩大。
2. 影响范围：告警灵敏度与跨环境一致性。
3. 缓解措施：保持 CI `--check` 强制执行，并要求配置变更走评审。

## 8. 关键决策

1. 决策内容：继续沿用单配置入口，不拆分 governance 独立配置。
2. 决策原因：降低操作复杂度，保持同步脚本实现集中。
3. 影响模块：告警模板治理和配置变更流程。

## 9. 下迭代计划

1. 评估 governance ratio 阈值与告警模板参数统一化方案。
2. 增加同步脚本 dry-run 差异输出，提升审阅可读性。
3. 补充配置变更审批 checklist（字段影响矩阵）。
