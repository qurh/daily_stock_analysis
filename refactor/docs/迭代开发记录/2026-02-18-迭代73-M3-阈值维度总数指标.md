# 迭代开发记录

迭代编号：`迭代73`  
日期：`2026-02-18`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 为阈值偏差治理补充“维度总数”可观测指标。
2. 明确 mismatch ratio 的分母语义，降低后续扩展时的认知偏差风险。
3. 保持现有 count/ratio 指标和接口兼容。

## 2. 计划范围（Plan）

1. 新增维度总数指标测试并按 TDD 实现。
2. 在 backtest quality 快照中输出阈值维度总数字段。
3. 在 `/api/v2/metrics` 导出 gauge 指标。
4. 同步版本与文档。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_multi_window_alert_threshold_dimensions_total`
2. 逻辑增强：
   - 在阈值偏差计算路径复用四维分母并显式输出维度总数（当前为 4）。
3. 测试增强：
   - 在 `test_global_metrics_endpoint_includes_raw_normalized_mismatch_count` 增加维度总数断言。
   - 按 TDD 验证新增断言先失败后通过。
4. 版本升级：
   - `0.3.58-m3-global-metrics-threshold-dimensions-total`

## 4. 未完成项（Not Done）

1. 暂未将维度总数抽取为统一模块级常量。
2. 暂未提供“维度变更审计”事件日志。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-18-迭代73-M3-阈值维度总数指标.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_prompt_lock_audit.py -k "raw_normalized_mismatch_count" -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（新增断言先失败后通过）
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（阈值维度总数可观测）

## 7. 风险与问题

1. 风险描述：若未来阈值维度动态扩展，固定值 4 需要同步更新。
2. 影响范围：ratio 分母语义与告警阈值策略。
3. 缓解措施：后续引入统一常量与单点定义，配套回归测试。

## 8. 关键决策

1. 决策内容：先输出单一总数字段，不新增标签化维度明细。
2. 决策原因：维持指标基数稳定，优先支撑治理语义清晰。
3. 影响模块：metrics 快照、Prometheus 指标导出、告警规则设计。

## 9. 下迭代计划

1. 将阈值维度总数抽取为统一常量并补充单元测试。
2. 设计 mismatch count/ratio/total 组合告警策略模板。
3. 评估 dashboard 侧增加阈值治理解释提示文案。
