# 迭代开发记录

迭代编号：`迭代77`  
日期：`2026-02-18`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 让治理比例阈值（warn/critical）的归一化状态可观测。
2. 在 settings 层明确记录“是否发生归一化”。
3. 将归一化标记导出为 metrics，便于排查告警阈值行为。

## 2. 计划范围（Plan）

1. 先补 settings 与 metrics 的失败测试。
2. 在 settings 增加治理比例归一化标记字段与计算逻辑。
3. 在 `/api/v2/metrics` 导出归一化标记指标。
4. 同步版本与文档。

## 3. 实际完成（Done）

1. settings 新增字段：
   - `backtest_multi_window_alert_threshold_governance_warn_ratio_normalized`
   - `backtest_multi_window_alert_threshold_governance_critical_ratio_normalized`
   - `backtest_multi_window_alert_threshold_governance_ratio_normalization_applied`
2. 指标新增：
   - `refactor_backtest_records_return_sample_multi_window_alert_threshold_governance_warn_ratio_normalized`
   - `refactor_backtest_records_return_sample_multi_window_alert_threshold_governance_critical_ratio_normalized`
   - `refactor_backtest_records_return_sample_multi_window_alert_threshold_governance_ratio_normalization_applied`
3. 测试增强：
   - `test_settings_clamps_governance_ratio_range`
   - `test_global_metrics_endpoint_includes_threshold_governance_ratio_normalization_flags`
   - 扩展现有 settings / governance override 用例的归一化断言
4. 版本升级：
   - `0.3.62-m3-governance-ratio-normalization-flags`

## 4. 未完成项（Not Done）

1. 暂未输出治理比例原始值（raw）指标。
2. 暂未将归一化事件写入审计日志。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/core/settings.py`
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_settings_env_names.py`
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-18-迭代77-M3-治理比例归一化标记.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_settings_env_names.py tests/unit/test_prompt_lock_audit.py -k "governance_ratio_normalization or governance_levels_support_env_ratio_overrides or settings" -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（新增测试先失败后通过）
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（治理比例归一化状态可观测）

## 7. 风险与问题

1. 风险描述：仅有归一化标记，尚无 raw 比例指标。
2. 影响范围：复杂配置场景下的溯源粒度。
3. 缓解措施：后续可补 raw 指标或审计日志事件。

## 8. 关键决策

1. 决策内容：先落地归一化标记，不新增 raw 指标。
2. 决策原因：以最小指标增量提升可观测性，避免指标爆炸。
3. 影响模块：settings、metrics 导出、告警调试流程。

## 9. 下迭代计划

1. 产出 Prometheus 告警规则模板（治理等级 + 归一化标记联动）。
2. 评估是否补充治理比例 raw 指标用于完整审计。
3. 在 README 增加阈值调参建议区间示例。
