# 迭代开发记录

迭代编号：`迭代144`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 为 marker 配置校验器补齐 `--json-errors` 结构化错误输出能力。
2. 统一错误码与上下文字段，便于后续前端/自动化消费。

## 2. 计划范围（Plan）

1. 先补失败测试，定义 `--json-errors` 输出契约。
2. 改造 marker 校验脚本引入错误码模型并输出 JSON 错误。
3. 同步文档与版本，完成全量验证。

## 3. 实际完成（Done）

1. TDD Red：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - 新增失败约束：
     - duplicate marker 在 `--json-errors` 下返回结构化 JSON
     - schema 违例在 `--json-errors` 下返回结构化 JSON
2. TDD Green：
   - `refactor/backend/scripts/validate-validator-placeholder-markers.py`
   - 新增 `--json-errors` 参数。
   - 引入 `PlaceholderMarkersValidationError`，统一 `code/message/context`。
   - 关键错误码：
     - `placeholder_markers_duplicate_marker`
     - `placeholder_markers_schema_validation_failed`
     - 以及文件缺失/JSON 解析/schema 非法等同命名空间错误码
3. 文档与版本：
   - `refactor/backend/README.md` 增加 `--json-errors` 用法说明。
   - `refactor/docs/CHANGELOG.md` 新增 `0.3.129-m3-marker-validator-json-errors`。
   - `refactor/backend/src/app/main.py` 版本升级至 `0.3.129-m3-marker-validator-json-errors`。

## 4. 未完成项（Not Done）

1. 暂未将 marker 校验器 `--json-errors` 纳入独立 contract 测试脚本。
2. 暂未提供错误码目录对 marker 校验器错误码的统一收录。

## 5. 代码与文档变更

1. 脚本：
   - `refactor/backend/scripts/validate-validator-placeholder-markers.py`
2. 测试：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代144-M3-marker校验器json错误输出.md`

## 6. 验证记录

1. Red 阶段：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "json_errors_for_duplicate_markers or json_errors_for_schema_violation" -q`
   - 结果：预期失败（脚本不支持 `--json-errors`）。
2. Green 阶段：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "json_errors_for_duplicate_markers or json_errors_for_schema_violation" -q`
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "placeholder_markers_validator_script" -q`
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
   - 结果：全部通过。
3. 是否达到验收标准：
   - 达到（marker 校验器支持结构化错误输出并有测试保护）。

## 7. 风险与问题

1. 风险描述：新增错误码未并入统一错误码目录治理链路。
2. 影响范围：错误码统一管理完整性。
3. 缓解措施：后续将该脚本错误码纳入统一 catalog 与覆盖测试。

## 8. 关键决策

1. 决策内容：沿用既有 validator 的 `validator/code/message/context` JSON 错误结构。
2. 决策原因：降低接入成本，保持治理风格一致。
3. 影响模块：marker 配置校验器与自动化消费接口。

## 9. 下迭代计划

1. 将 marker 校验器错误码并入统一错误码目录与覆盖校验。
2. 为 marker 校验器增加 contract/changelog 关联校验。
3. 推进错误码目录结构化字段（severity/remediation）落地与校验。
