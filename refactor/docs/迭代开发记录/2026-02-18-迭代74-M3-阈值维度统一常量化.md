# 迭代开发记录

迭代编号：`迭代74`  
日期：`2026-02-18`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 消除阈值治理路径中的 magic number（`4`）。
2. 将阈值维度总数统一为单点常量，降低维护风险。
3. 保持现有指标名称与语义不变。

## 2. 计划范围（Plan）

1. 新增测试，要求维度总数指标与模块常量保持一致。
2. 在 metrics 模块中定义统一常量并替换原硬编码值。
3. 同步版本与文档。

## 3. 实际完成（Done）

1. 新增模块常量：
   - `MULTI_WINDOW_ALERT_THRESHOLD_DIMENSIONS_TOTAL = 4`
2. 逻辑调整：
   - mismatch ratio 分母改为引用统一常量，不再使用 magic number。
3. 测试增强：
   - `test_global_metrics_endpoint_includes_raw_normalized_mismatch_count` 改为基于常量断言维度总数指标。
   - 按 TDD 验证新增约束先失败后通过。
4. 版本升级：
   - `0.3.59-m3-global-metrics-threshold-dimensions-constant`

## 4. 未完成项（Not Done）

1. 暂未把该常量抽取到跨模块共享常量文件。
2. 暂未引入维度扩展时的自动迁移或告警机制。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-18-迭代74-M3-阈值维度统一常量化.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_prompt_lock_audit.py -k "raw_normalized_mismatch_count" -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（先失败后通过）
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（分母语义统一且可回归验证）

## 7. 风险与问题

1. 风险描述：当前常量仍位于路由模块，后续若多个模块引用可能出现耦合。
2. 影响范围：跨模块扩展的可维护性。
3. 缓解措施：后续评估抽取到 shared constants 模块。

## 8. 关键决策

1. 决策内容：先做单模块统一常量，避免过早抽象。
2. 决策原因：当前仅该模块使用，保持改动最小且可验证。
3. 影响模块：metrics 路由与指标测试。

## 9. 下迭代计划

1. 产出 mismatch 指标组合告警规则模板（count/ratio/total）。
2. 在 docs 中增加 Prometheus 规则示例和阈值建议。
3. 评估是否需要将治理常量迁移到共享配置层。
