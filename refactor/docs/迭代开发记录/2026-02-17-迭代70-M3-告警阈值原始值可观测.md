# 迭代开发记录

迭代编号：`迭代70`  
日期：`2026-02-17`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 输出 multi-window alert 阈值“归一化前原始值”指标。
2. 形成“原始值 + 生效值 + 归一化标记”的审计闭环。
3. 在最小改动下完成可回归验证。

## 2. 计划范围（Plan）

1. 新增原始阈值指标测试并按 TDD 实现。
2. 在 settings 中保留 raw 阈值字段。
3. 在 metrics 中导出 raw 阈值指标。

## 3. 实际完成（Done）

1. 新增指标：
   - `refactor_backtest_records_return_sample_multi_window_alert_warn_low_windows_threshold_raw`
   - `refactor_backtest_records_return_sample_multi_window_alert_warn_threshold_unmet_windows_threshold_raw`
   - `refactor_backtest_records_return_sample_multi_window_alert_critical_low_windows_threshold_raw`
   - `refactor_backtest_records_return_sample_multi_window_alert_critical_threshold_unmet_windows_threshold_raw`
2. 逻辑增强：
   - settings 新增四个 raw 阈值字段，保留配置输入值。
   - metrics 快照输出 raw 与 normalized 两套阈值，支持差异审计。
3. 测试增强：
   - 新增 `test_global_metrics_endpoint_includes_raw_multi_window_alert_thresholds`
   - 扩展 settings env 测试断言 raw 字段
   - 按 TDD 验证新增用例先失败后通过
4. 版本升级：
   - `0.3.55-m3-global-metrics-alert-threshold-raw-values`

## 4. 未完成项（Not Done）

1. 暂未输出“raw 与 normalized 差值”指标。
2. 暂未提供原始阈值历史版本追踪。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/src/app/core/settings.py`
   - `refactor/backend/src/app/api/routes/metrics.py`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_prompt_lock_audit.py`
   - `refactor/backend/tests/unit/test_settings_env_names.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-17-迭代70-M3-告警阈值原始值可观测.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_prompt_lock_audit.py -k "raw_multi_window_alert_thresholds" -q`
   - `cd refactor/backend && pytest tests/unit/test_settings_env_names.py -q`
   - `cd refactor/backend && pytest tests/unit --maxfail=1`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（新增用例先失败后通过）
   - 全量单测：通过
   - CI：通过
3. 是否达到验收标准：
   - 达到（阈值原始值可观测）

## 7. 风险与问题

1. 风险描述：指标数量增长，监控面板复杂度继续上升。
2. 影响范围：告警规则编写与仪表盘维护。
3. 缓解措施：后续整理告警模板，预置标准规则表达式。

## 8. 关键决策

1. 决策内容：raw 阈值直接使用配置输入值，不做二次修正。
2. 决策原因：最大化保留配置原貌，便于审计和排障。
3. 影响模块：settings 装载、metrics 导出。

## 9. 下迭代计划

1. 评估增加“raw vs normalized mismatch”单指标。
2. 评估输出阈值治理告警模板文档（含 PromQL 示例）。
3. 评估阈值异常启动日志提示优化。
