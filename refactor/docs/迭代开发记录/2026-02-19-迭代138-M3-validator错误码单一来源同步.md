# 迭代开发记录

迭代编号：`迭代138`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 建立 validator 错误码目录的自动同步机制，降低人工双维护风险。
2. 在 CI 中新增 drift 检查，确保目录与脚本注册表持续一致。

## 2. 计划范围（Plan）

1. 先用测试定义同步脚本行为与 CI 接入约束。
2. 实现 `sync-validator-error-codes.py` 并支持 `--check` 模式。
3. 同步文档与版本，完成全量验证。

## 3. 实际完成（Done）

1. TDD Red：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
   - 新增失败约束：
     - `ci.sh` 必须调用 `sync-validator-error-codes.py --check`
     - 同步脚本默认目录必须通过 `--check`
     - 目录漂移时 `--check` 必须失败
2. TDD Green：
   - 新增脚本：
     - `refactor/backend/scripts/sync-validator-error-codes.py`
   - 能从两类 validator 的 `VALIDATOR_ERROR_CODES` 生成目录，并保留已有描述。
   - 支持 `--check` 漂移检测，返回非零退出码。
3. CI 接入：
   - `refactor/backend/scripts/ci.sh` 新增错误码目录同步检查步骤。
4. 文档与版本：
   - `refactor/backend/README.md` 增加 sync/check 命令说明。
   - `refactor/docs/CHANGELOG.md` 新增 `0.3.123-m3-validator-error-code-sync`。
   - `refactor/backend/src/app/main.py` 版本升级至 `0.3.123-m3-validator-error-code-sync`。

## 4. 未完成项（Not Done）

1. 未实现 catalog 描述字段的强约束（如 remediation/severity）。
2. 未实现“目录描述缺失自动失败”模式（当前默认填充 TODO）。

## 5. 代码与文档变更

1. 脚本：
   - `refactor/backend/scripts/sync-validator-error-codes.py`
   - `refactor/backend/scripts/ci.sh`
2. 配置：
   - `refactor/backend/config/validator-error-codes.json`
3. 测试：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
4. 文档：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代138-M3-validator错误码单一来源同步.md`

## 6. 验证记录

1. Red 阶段：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "sync_script or invokes_prometheus_rules_check" -q`
   - 结果：预期失败（CI 未接入且同步脚本未实现）。
2. Green 阶段：
   - `cd refactor/backend && python3 scripts/sync-validator-error-codes.py`
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -k "sync_script or invokes_prometheus_rules_check" -q`
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
   - 结果：全部通过。
3. 是否达到验收标准：
   - 达到（错误码目录进入可检查的自动同步链路）。

## 7. 风险与问题

1. 风险描述：如果新增错误码但未补充描述，当前会填充 TODO 文案。
2. 影响范围：错误码说明质量。
3. 缓解措施：后续加 `--strict-descriptions` 并在 CI 启用。

## 8. 关键决策

1. 决策内容：以 validator 脚本注册表作为错误码集合来源，catalog 作为描述载体。
2. 决策原因：可最小改动接入现有架构，同时解决集合漂移问题。
3. 影响模块：validator 治理脚本、CI、文档。

## 9. 下迭代计划

1. 增加描述质量校验（禁止 TODO 占位）并接入 CI。
2. 评估将错误码目录生成为前端/告警可消费的 API 文档。
3. 继续推进错误码治理到其他校验脚本模块。
