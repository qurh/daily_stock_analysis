# 迭代开发记录

迭代编号：`迭代91`  
日期：`2026-02-19`  
负责人：`Codex + qrh`

---

## 1. 本迭代目标

1. 将 promtool 的版本与 checksum 从脚本/workflow 中集中到统一配置文件。
2. 降低版本升级时多处改动导致的不一致风险。
3. 保持 installer 的 dry-run 与多架构能力可用。

## 2. 计划范围（Plan）

1. 先补失败测试，约束配置文件存在且 workflow 不再硬编码版本/checksum。
2. 新增集中配置文件并改造安装脚本读取配置。
3. 更新版本与文档。

## 3. 实际完成（Done）

1. 新增集中配置文件：
   - `refactor/backend/config/promtool-installer.defaults`
   - 包含：
     - `PROMTOOL_DEFAULT_VERSION`
     - `PROMTOOL_DEFAULT_SHA256_LINUX_AMD64`
     - `PROMTOOL_DEFAULT_SHA256_LINUX_ARM64`
2. 安装脚本改造：
   - `refactor/backend/scripts/install-promtool.sh`
   - 新增 `PROMTOOL_CONFIG_FILE` 配置文件路径机制，脚本启动时 `source` 配置。
   - 默认值从配置文件读取，环境变量仍可覆盖。
3. Workflow 改造：
   - `.github/workflows/refactor-backend-ci.yml`
   - `refactor/backend/ci/github-actions/refactor-backend-ci.example.yml`
   - 去除内联 `PROMTOOL_VERSION` / `PROMTOOL_SHA256` 硬编码。
4. 版本升级：
   - `0.3.76-m3-promtool-installer-centralized-config`

## 4. 未完成项（Not Done）

1. 暂未将该配置提取为跨项目共享配置源（当前为 backend 局部配置）。
2. 暂未增加配置变更自动校验流程（如 checksum 更新助手）。

## 5. 代码与文档变更

1. 代码路径：
   - `refactor/backend/config/promtool-installer.defaults`
   - `refactor/backend/scripts/install-promtool.sh`
   - `.github/workflows/refactor-backend-ci.yml`
   - `refactor/backend/ci/github-actions/refactor-backend-ci.example.yml`
   - `refactor/backend/src/app/main.py`
2. 测试路径：
   - `refactor/backend/tests/unit/test_ci_prometheus_rules_check.py`
3. 文档路径：
   - `refactor/backend/README.md`
   - `refactor/docs/CHANGELOG.md`
   - `refactor/docs/迭代开发记录/2026-02-19-迭代91-M3-promtool安装配置集中化.md`

## 6. 验证记录

1. 执行命令：
   - `cd refactor/backend && pytest tests/unit/test_ci_prometheus_rules_check.py -q`
   - `cd refactor/backend && bash scripts/ci.sh`
2. 结果摘要：
   - 定向测试：通过（11 passed）
   - CI 脚本：通过
3. 是否达到验收标准：
   - 达到（配置集中化完成，且行为受测试约束）

## 7. 风险与问题

1. 风险描述：配置文件被误改会影响安装链路。
2. 影响范围：CI 中 promtool 安装与规则校验门禁。
3. 缓解措施：后续可增加配置校验脚本与变更审计。

## 8. 关键决策

1. 决策内容：采用 shell env 配置文件集中管理安装参数。
2. 决策原因：脚本天然兼容，改造成本低，便于环境变量覆盖。
3. 影响模块：installer 脚本、CI workflow、文档与测试基线。

## 9. 下迭代计划

1. 增加配置文件合法性检查（版本格式、checksum 长度等）。
2. 评估自动更新 promtool 配置的辅助脚本。
3. 持续推进 M3 治理链路稳定性提升。
